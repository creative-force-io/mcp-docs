{
  "schemaVersion": 1,
  "label": "tools",
  "message": "16 tools",
  "color": "blue",
  "toolCount": 16,
  "tools": [
    {
      "name": "get_asset_preview",
      "title": "Get Asset Preview",
      "module": "Assets",
      "description": "Get inline image previews for digital assets so the user can see them. Use when the user wants to SEE / VIEW / DISPLAY / RENDER specific assets or images. Accepts a list of asset IDs — typically obtained from query_asset (or any other tool that returns asset IDs). Returns a JSON object with a `previews` array of { assetId, url } — render each as ![assetId](url) in your reply so the user can see the images. URLs are short-lived presigned links. Do NOT use for searching, listing, or counting assets — use query_asset for that.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "assetIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Asset IDs to fetch preview images for. Pass as a JSON array of strings, e.g. [\"id1\",\"id2\"]. Get IDs from query_asset or any tool that returns assetIds."
        }
      ],
      "exampleInput": "{ \"assetIds\": [\"a1b2c3\", \"d4e5f6\"] }",
      "exampleOutput": "{ \"previews\": [ { \"assetId\": \"a1b2c3\", \"url\": \"https://cdn.../a1b2c3.jpg?sig=...\", \"fileName\": \"shot_001.jpg\" } ], \"missing\": [], \"truncated\": false }",
      "knownLimitations": "Hard cap of 200 asset IDs per call (extras are dropped and `truncated: true` is returned). Preview URLs are short-lived presigned links that expire. Returns images only — use query_asset to search/list/count."
    },
    {
      "name": "get_talent_crew_preview",
      "title": "Get Talent & Crew Preview",
      "module": "Talent & Crew",
      "description": "Inline avatar GALLERY widget for a list of talent crew IDs. **TRIGGER STRICTLY when user prompt is purely visual** — e.g. 'show me their faces', 'what do they look like', 'photo gallery of these models'. **DO NOT TRIGGER**:   • after `query_talent_crew` (its table widget already shows Photo column)   • after `query_talent_crew_schedule` (its widget renders avatars inline post-improvements-v1)   • for 'show profile' / 'who is X' / 'list talents' (use `query_talent_crew`) **Best fit**: standalone visual question without data context. Returns `previews` array of { talentCrewId, displayName, avatarUrl, deepLinkUrl }. Widget owns visual — text response = ONE sentence ('Showing N avatars.').",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "talentCrewIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Talent crew IDs to fetch avatar previews for. Pass as a JSON array of strings, e.g. [\"id1\",\"id2\"]."
        }
      ],
      "exampleInput": "{ \"talentCrewIds\": [\"t-101\", \"t-102\"] }",
      "exampleOutput": "{ \"previews\": [ { \"talentCrewId\": \"t-101\", \"name\": \"Jane Doe\", \"avatarUrl\": \"https://cdn.../t-101.jpg?sig=...\" } ], \"missing\": [] }",
      "knownLimitations": "Visual-only avatar gallery. Do NOT chain after query_talent_crew or query_talent_crew_schedule — those already render avatars. Avatars are resolved in chunks of 50; talent without an avatarId are omitted."
    },
    {
      "name": "query_asset",
      "title": "Query Asset",
      "module": "Assets",
      "description": "List or count/aggregate digital asset metadata (id, name, step, dates) — does NOT return preview images. Use when the user wants to FIND, LIST, or COUNT assets/photos/shots/deliverables. Filter by job, product, production type, step, workspace, photographer, stylist, model, star rating, time range. Drill-down: pass workUnitIds/jobIds/productIds from upstream queries. When groupBy params set, returns counts/aggregations instead of list. Default: latest step only when step not specified. Use names for filters (production type, step). IDs accepted as fallback. If the user wants to SEE/VIEW/RENDER the images, follow up with the get_asset_preview tool using the returned asset IDs.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code"
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name"
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code (exact)"
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name (exact)"
        },
        {
          "name": "product",
          "type": "string",
          "required": false,
          "description": "Search product by code or name (OR)"
        },
        {
          "name": "productionType",
          "type": "string",
          "required": false,
          "description": "Production type name (e.g. Flatlay, On-Model) or ID"
        },
        {
          "name": "step",
          "type": "string",
          "required": false,
          "description": "Step name: Photography, InternalPostProduction, AssetDelivery, etc."
        },
        {
          "name": "dateFrom",
          "type": "string",
          "required": false,
          "description": "Task finished date from (ISO)"
        },
        {
          "name": "dateTo",
          "type": "string",
          "required": false,
          "description": "Task finished date to (ISO)"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID"
        },
        {
          "name": "photographer",
          "type": "string",
          "required": false,
          "description": "Photographer name or user ID"
        },
        {
          "name": "videographer",
          "type": "string",
          "required": false,
          "description": "Videographer name or user ID"
        },
        {
          "name": "stylist",
          "type": "string",
          "required": false,
          "description": "Stylist name or user ID"
        },
        {
          "name": "model",
          "type": "string",
          "required": false,
          "description": "Model name or user ID"
        },
        {
          "name": "artDirector",
          "type": "string",
          "required": false,
          "description": "Art director name or user ID"
        },
        {
          "name": "hairAndMakeup",
          "type": "string",
          "required": false,
          "description": "Hair & Makeup artist name or user ID"
        },
        {
          "name": "assistant",
          "type": "string",
          "required": false,
          "description": "Assistant on set name or user ID"
        },
        {
          "name": "externalPostVendor",
          "type": "string",
          "required": false,
          "description": "External post-production vendor name or ID"
        },
        {
          "name": "starRating",
          "type": "int?",
          "required": false,
          "description": "Star rating (0-5). 0 = no rating assigned"
        },
        {
          "name": "isVariant",
          "type": "bool?",
          "required": false,
          "description": "Only variant assets"
        },
        {
          "name": "heroOnly",
          "type": "bool?",
          "required": false,
          "description": "Only hero/primary assets"
        },
        {
          "name": "isAlternative",
          "type": "bool?",
          "required": false,
          "description": "Only alternative assets"
        },
        {
          "name": "isLatestStep",
          "type": "bool?",
          "required": false,
          "description": "Latest step only (default true when step omitted)"
        },
        {
          "name": "workUnitIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by work-unit (production) GUIDs. JSON array. Drill-down from query_ecomm_production."
        },
        {
          "name": "jobIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by job GUIDs. JSON array. Drill-down from query_ecomm_job."
        },
        {
          "name": "productIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by product GUIDs. JSON array. Drill-down from query_ecomm_product_request."
        },
        {
          "name": "editorialProjectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter assets to one or more Editorial Projects. Pass as JSON array of projectId GUID strings. Drill-down: pass projectIds returned from query_editorial_project. Editorial-asset only — Asset doc carries editorialProjectId denormalized when contentCreationTypeId=2."
        },
        {
          "name": "editorialDeliverableName",
          "type": "string",
          "required": false,
          "description": "Filter editorial assets by Editorial Deliverable name. Exact match, lowercased. Matches assets that are EITHER assigned to a deliverable with this name (pre-finalize: nested editorialAssignedToDeliverables) OR whose parent workUnit finalizes to a deliverable with this name (post-finalize: has_parent.editorialDeliverableOfFinalizeWorkflow). Editorial-asset only."
        },
        {
          "name": "editorialDeliverableCode",
          "type": "string",
          "required": false,
          "description": "Filter editorial assets by Editorial Deliverable code (e.g. 'ED105426CF'). Exact match, lowercased. Same OR-of-paths semantics as editorialDeliverableName."
        },
        {
          "name": "editorialDeliverableOutputName",
          "type": "string",
          "required": false,
          "description": "Filter editorial assets by Editorial Deliverable output name (publishing/output label). Exact match, lowercased. Same OR-of-paths semantics as editorialDeliverableName."
        },
        {
          "name": "editorialDeliverableDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Filter editorial assets by Editorial Deliverable due-date — earliest. ISO date or epoch ms. Pair with editorialDeliverableDueDateTo. Same OR-of-paths semantics as editorialDeliverableName."
        },
        {
          "name": "editorialDeliverableDueDateTo",
          "type": "string",
          "required": false,
          "description": "Filter editorial assets by Editorial Deliverable due-date — latest. ISO date or epoch ms. Inclusive (`lte`). Same OR-of-paths semantics as editorialDeliverableName."
        },
        {
          "name": "editorialDeliverableCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable custom property name (e.g. 'season') or its GUID. Pair with editorialDeliverableCustomPropertyValue. Resolved via masterDataCache.GetEditorialDeliverableCustomPropertiesAsync (objectType=101). Same OR-of-paths semantics."
        },
        {
          "name": "editorialDeliverableCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for editorialDeliverableCustomPropertyName. Required when name is set. Exact match, lowercased."
        },
        {
          "name": "groupByStep",
          "type": "bool",
          "required": false,
          "description": "Group by step — aggregation mode"
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Group by period: hour, day, week, month — aggregation mode"
        },
        {
          "name": "groupByProductionType",
          "type": "bool",
          "required": false,
          "description": "Group by production type — aggregation mode"
        },
        {
          "name": "groupByStarRating",
          "type": "bool",
          "required": false,
          "description": "Group by star rating — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. Use for any 'per workspace', 'per client', 'by workspace' question."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page 0-based (ignored in aggregation)"
        }
      ],
      "exampleInput": "{ \"step\": \"Photography\", \"starRating\": 5, \"dateFrom\": \"2026-06-01\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 87, \"hits\": [ { \"assetId\": \"a1b2...\", \"name\": \"shot_001.jpg\", \"step\": \"Photography\", \"starRating\": 5 } ] }",
      "knownLimitations": "Returns metadata only — no image URLs. To display images, pass these assetIds to get_asset_preview. When any groupBy* is set it returns aggregation buckets (counts) instead of a hit list."
    },
    {
      "name": "query_ecomm_job",
      "title": "Query E-Comm Job",
      "module": "Jobs",
      "description": "Search production jobs — containers that group products for production. Use this to answer: what jobs exist, job status, deadline, how many jobs by status. Filter by code or name (use 'job' param for OR search), status, workspace, deadline range. Pass jobIds to look up specific jobs by ID (e.g. from detail page context). Supports custom property filters via customPropertyName + customPropertyValue. When groupBy params set, returns counts/aggregations instead of list.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "jobIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Job GUIDs — look up specific jobs by ID. JSON array or single ID. Use when navigating from a Job detail page."
        },
        {
          "name": "job",
          "type": "string",
          "required": false,
          "description": "Search job by code or name (OR)"
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code (exact)"
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name (exact)"
        },
        {
          "name": "jobStatusName",
          "type": "string",
          "required": false,
          "description": "Job status name"
        },
        {
          "name": "jobStatusId",
          "type": "int?",
          "required": false,
          "description": "Job status ID (if known)"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID"
        },
        {
          "name": "deadlineFrom",
          "type": "string",
          "required": false,
          "description": "Deadline from (epoch ms or ISO)"
        },
        {
          "name": "deadlineTo",
          "type": "string",
          "required": false,
          "description": "Deadline to (epoch ms or ISO)"
        },
        {
          "name": "customPropertyName",
          "type": "string",
          "required": false,
          "description": "Custom property name (resolved to GUID) or GUID directly"
        },
        {
          "name": "customPropertyValue",
          "type": "string",
          "required": false,
          "description": "Custom property value to match (exact)"
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by status — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. Use for any 'per workspace', 'per client', 'by workspace' question."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        }
      ],
      "exampleInput": "{ \"jobStatusName\": \"In Production\", \"deadlineTo\": \"2026-06-30\", \"pageSize\": 5 }",
      "exampleOutput": "{ \"total\": 12, \"hits\": [ { \"jobId\": \"f14e694e-...\", \"jobCode\": \"JOB1042\", \"jobName\": \"SS26 Campaign\", \"jobStatusName\": \"In Production\", \"deadline\": \"2026-06-28T00:00:00Z\" } ] }",
      "knownLimitations": "Returns job containers only — not the products inside them (use query_ecomm_product_request for those)."
    },
    {
      "name": "query_ecomm_product_request",
      "title": "Query E-Comm Product Request",
      "module": "Product Hub",
      "description": "Search products in the catalog. Filter by code, name, job, category, brand, color, style code, vendor material code, status, workspace, product state (Active/Inactive), sample check-in status, sample code, sample location, sample check-in date, pool first-check-in date, product created date, container code, product tagging, custom property. Status: Backlog, Todo, InProgress, Done. Use product param to search both productCode and productName (OR). Pass productIds to look up specific products by ID (e.g. from detail page context). When any groupBy* param is set, returns aggregation buckets instead of a list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true — do not try to derive workspace names from list results.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "productIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Product GUIDs — look up specific products by ID. JSON array or single ID. Use when navigating from a Product detail page."
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code (exact)"
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name (exact)"
        },
        {
          "name": "product",
          "type": "string",
          "required": false,
          "description": "Search product by code or name (OR)"
        },
        {
          "name": "jobIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Job GUIDs — filter products belonging to specific jobs. JSON array or single ID. Drill-down from query_ecomm_job or Job detail page context."
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code"
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name"
        },
        {
          "name": "category",
          "type": "string",
          "required": false,
          "description": "Category"
        },
        {
          "name": "brand",
          "type": "string",
          "required": false,
          "description": "Brand"
        },
        {
          "name": "color",
          "type": "string",
          "required": false,
          "description": "Color"
        },
        {
          "name": "styleCode",
          "type": "string",
          "required": false,
          "description": "Style code"
        },
        {
          "name": "vendorMaterialCode",
          "type": "string",
          "required": false,
          "description": "Vendor material code"
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "Status: Backlog, Todo, InProgress, Done"
        },
        {
          "name": "statusId",
          "type": "int?",
          "required": false,
          "description": "Status ID (if known)"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID"
        },
        {
          "name": "customPropertyName",
          "type": "string",
          "required": false,
          "description": "Custom property name (resolved to GUID) or GUID directly"
        },
        {
          "name": "customPropertyValue",
          "type": "string",
          "required": false,
          "description": "Custom property value to match (exact)"
        },
        {
          "name": "editorialProjectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Editorial Project — projects this product is associated with. Pass projectId GUID array (drill-down: pass projectIds returned from query_editorial_project). Multi-value — product matches if ANY of its associated projects are in the list."
        },
        {
          "name": "productState",
          "type": "string",
          "required": false,
          "description": "Product state: 'Active' (in production) or 'Inactive' (ignored/excluded)."
        },
        {
          "name": "sampleCheckInStatus",
          "type": "string",
          "required": false,
          "description": "Sample check-in status: 'CheckedIn' (at least one sample at studio) or 'NotCheckedIn' (no sample arrived yet)."
        },
        {
          "name": "sampleCode",
          "type": "string",
          "required": false,
          "description": "Sample code in the product's pool (exact match)."
        },
        {
          "name": "sampleLocation",
          "type": "string",
          "required": false,
          "description": "Location/sub-location where the product's samples are stored. Name or ID."
        },
        {
          "name": "sampleCheckedInDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest sample check-in date. ISO date or epoch ms."
        },
        {
          "name": "sampleCheckedInDateTo",
          "type": "string",
          "required": false,
          "description": "Latest sample check-in date. Pair with sampleCheckedInDateFrom."
        },
        {
          "name": "containerCode",
          "type": "string",
          "required": false,
          "description": "Container/box code where the product's sample is stored."
        },
        {
          "name": "poolFirstCheckedInDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest pool first-check-in date — when the first sample ever arrived for this product. ISO date or epoch ms."
        },
        {
          "name": "poolFirstCheckedInDateTo",
          "type": "string",
          "required": false,
          "description": "Latest pool first-check-in date. Pair with poolFirstCheckedInDateFrom."
        },
        {
          "name": "productCreatedDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest product creation date. ISO date or epoch ms."
        },
        {
          "name": "productCreatedDateTo",
          "type": "string",
          "required": false,
          "description": "Latest product creation date."
        },
        {
          "name": "isProductTagged",
          "type": "bool?",
          "required": false,
          "description": "Whether the product has been tagged in assets."
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by status — aggregation mode"
        },
        {
          "name": "groupByCategory",
          "type": "bool",
          "required": false,
          "description": "Group by category — aggregation mode"
        },
        {
          "name": "groupByBrand",
          "type": "bool",
          "required": false,
          "description": "Group by brand — aggregation mode"
        },
        {
          "name": "groupByJob",
          "type": "bool",
          "required": false,
          "description": "Group by job — aggregation mode"
        },
        {
          "name": "groupByColor",
          "type": "bool",
          "required": false,
          "description": "Group by color — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. MUST be set to true for any 'per workspace', 'per client', 'by workspace', 'breakdown by client' question. Returns counts by workspace name. Do NOT attempt to group by workspace manually from list results — use this param to get accurate server-side aggregation."
        },
        {
          "name": "groupByCustomProperty",
          "type": "bool",
          "required": false,
          "description": "Group by the value of customPropertyName — aggregation mode. Requires customPropertyName to be set."
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Time-bucket aggregation: hour, day, week, or month. Pair with groupByPeriodField to choose which date drives the histogram."
        },
        {
          "name": "groupByPeriodField",
          "type": "string",
          "required": false,
          "description": "Date field for time-bucket histogram. Options: productCreated (default), poolFirstCheckedIn. Other pool date fields available for future use."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        }
      ],
      "exampleInput": "{ \"category\": \"Footwear\", \"status\": \"InProgress\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 34, \"hits\": [ { \"productId\": \"00010664-...\", \"productCode\": \"SKU-9001\", \"productName\": \"Runner Low\", \"category\": \"Footwear\", \"status\": \"InProgress\" } ] }",
      "knownLimitations": "Searches the product catalog — not physical samples (use query_sample) and not work-unit progress (use query_ecomm_production)."
    },
    {
      "name": "query_ecomm_production",
      "title": "Query E-Comm Production",
      "module": "Production",
      "description": "Search productions (work units) — the central entity tracking a product through the workflow. Use this to answer: what step is a production on, who's working on it, how many are done/in-progress, production counts by photographer/vendor/location/type, and turnaround times. Status: New, Todo, InProgress, Done. Step names: Photography, InternalPostProduction, FinalSelection, etc. Filter by job, product, sample, production type, vendor team, post-production vendor, photographer, stylist, model, art director, videographer, digital-processing user, location, workspace. **Job lookup param choice — strict per user phrasing:** caller says 'job code JOB-123' → `jobCode`; 'job named Spring Campaign' → `jobName`; 'job X' without saying code-vs-name → `job` (ambiguous, matches code OR name). Same pattern for product (`productCode`/`productName`/`product`). **Codes are EXACT (case-sensitive); names are case-insensitive.** **Sample lookup**: `sample` is ambiguous — matches sample UUID OR sampleCode (exact, case-sensitive) OR cfBarcode (exact, case-sensitive) OR sampleName (case-insensitive). Filter by planning session: `session` (ambiguous session UUID, code, or name partial — matches against planningSessionId OR planningSession.sessionCode OR planningSession.sessionName). Drill-down: pass jobIds/productIds/planningSessionIds from upstream queries. Use human-readable names for all filters (resolved to IDs automatically). Returns: current step & status, production type, products, job, samples (codes/names/cfBarcodes/sizes/total/checkedIn), team on set (all 8 roles), vendors, location, style guide, outfit (incl. styling item codes), planning session (id/name/code/start/end), timing. When groupBy params set, returns counts/aggregations instead of list.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "Status: New, Todo, InProgress, Done"
        },
        {
          "name": "step",
          "type": "string",
          "required": false,
          "description": "Current step name"
        },
        {
          "name": "stepStatus",
          "type": "string",
          "required": false,
          "description": "Current step status"
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code — EXACT match (case-sensitive). Use when caller explicitly says the job CODE."
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name — exact match, case-insensitive. Use when caller explicitly says the job NAME."
        },
        {
          "name": "job",
          "type": "string",
          "required": false,
          "description": "Job identifier when the caller said 'job X' WITHOUT specifying code-vs-name — matches jobCode (exact, case-sensitive) OR jobName (case-insensitive)."
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code — EXACT match (case-sensitive). Use when caller explicitly says the product CODE."
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name — exact match, case-insensitive. Use when caller explicitly says the product NAME."
        },
        {
          "name": "product",
          "type": "string",
          "required": false,
          "description": "Product identifier when the caller said 'product X' WITHOUT specifying code-vs-name — matches productCode (exact, case-sensitive) OR productName (case-insensitive)."
        },
        {
          "name": "sample",
          "type": "string",
          "required": false,
          "description": "Sample identifier — ambiguous match. UUID matches `samples.sampleIds`; sampleCode and cfBarcode match EXACT (case-sensitive); sampleName matches case-insensitive."
        },
        {
          "name": "productionType",
          "type": "string",
          "required": false,
          "description": "Production type name or ID"
        },
        {
          "name": "vendorTeam",
          "type": "string",
          "required": false,
          "description": "Vendor team name or ID"
        },
        {
          "name": "postProductionVendor",
          "type": "string",
          "required": false,
          "description": "Post-production vendor name or ID"
        },
        {
          "name": "photographer",
          "type": "string",
          "required": false,
          "description": "Photographer name or user ID"
        },
        {
          "name": "stylist",
          "type": "string",
          "required": false,
          "description": "Stylist name or user ID"
        },
        {
          "name": "model",
          "type": "string",
          "required": false,
          "description": "Model name or user ID"
        },
        {
          "name": "artDirector",
          "type": "string",
          "required": false,
          "description": "Art director name or user ID"
        },
        {
          "name": "hairAndMakeup",
          "type": "string",
          "required": false,
          "description": "Hair & makeup artist name or user ID"
        },
        {
          "name": "assistant",
          "type": "string",
          "required": false,
          "description": "Assistant name or user ID"
        },
        {
          "name": "videographer",
          "type": "string",
          "required": false,
          "description": "Videographer name or user ID"
        },
        {
          "name": "digitalProcessing",
          "type": "string",
          "required": false,
          "description": "Digital-processing operator name or user ID"
        },
        {
          "name": "session",
          "type": "string",
          "required": false,
          "description": "Planning session — UUID, code (e.g. 'SES137123CF'), or name partial. Matches planningSessionId OR planningSession.sessionCode OR planningSession.sessionName (wildcard contains, case-insensitive)."
        },
        {
          "name": "location",
          "type": "string",
          "required": false,
          "description": "Location/set name or ID"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID"
        },
        {
          "name": "styleGuide",
          "type": "string",
          "required": false,
          "description": "Style guide name or ID"
        },
        {
          "name": "outfitCode",
          "type": "string",
          "required": false,
          "description": "Outfit code (exact match)"
        },
        {
          "name": "jobIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by job GUIDs from upstream query_ecomm_job. JSON array or single ID."
        },
        {
          "name": "productIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by product GUIDs from upstream query_ecomm_product_request. JSON array or single ID."
        },
        {
          "name": "planningSessionIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter by planning session GUIDs from upstream query_planning. JSON array or single ID."
        },
        {
          "name": "dateFrom",
          "type": "string",
          "required": false,
          "description": "Finished date from (ISO)"
        },
        {
          "name": "dateTo",
          "type": "string",
          "required": false,
          "description": "Finished date to (ISO)"
        },
        {
          "name": "groupByStep",
          "type": "bool",
          "required": false,
          "description": "Group by current step — aggregation mode"
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by status — aggregation mode"
        },
        {
          "name": "groupByProductionType",
          "type": "bool",
          "required": false,
          "description": "Group by production type — aggregation mode"
        },
        {
          "name": "groupByLocation",
          "type": "bool",
          "required": false,
          "description": "Group by location — aggregation mode"
        },
        {
          "name": "groupByVendorTeam",
          "type": "bool",
          "required": false,
          "description": "Group by vendor team — aggregation mode"
        },
        {
          "name": "groupByPostProductionVendor",
          "type": "bool",
          "required": false,
          "description": "Group by post-production vendor — aggregation mode"
        },
        {
          "name": "groupByPhotographer",
          "type": "bool",
          "required": false,
          "description": "Group by photographer — aggregation mode"
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Group by period: hour, day, week, month — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. Use for any 'per workspace', 'per client', 'by workspace' question."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        }
      ],
      "exampleInput": "{ \"step\": \"Photography\", \"status\": \"InProgress\", \"photographer\": \"John Smith\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 18, \"hits\": [ { \"workUnitId\": \"c1...\", \"productCode\": \"SKU-9001\", \"step\": \"Photography\", \"status\": \"InProgress\", \"photographer\": \"John Smith\" } ] }",
      "knownLimitations": "The central work-unit entity. When any groupBy* is set it returns aggregation buckets (counts) instead of a hit list."
    },
    {
      "name": "query_editorial_deliverable",
      "title": "Query Editorial Deliverable",
      "module": "Editorial",
      "description": "Search Editorial Deliverables — the per-output unit inside an Editorial Project (each Deliverable is one delivery row, e.g. 'AEM Deliverable 1', 'D 20230627 3'). Does NOT return sample records — to list or find specific samples, use query_sample. Use to answer: which deliverables exist for a project/campaign, what step a deliverable is at, who owns it, what's overdue, how many at each step, deliverable counts by status/project/workspace/period. Filter by code, name, output-name, owner (full name), project (code/name/id), workspace, current step (Capture/Final Selection/External Post/External Post QC/Internal Post/Internal Post QC/Post Review/Asset Delivery), current step status (Backlog/Todo/InProgress/Done/Failed/Rejected), deliverable status, due-date range, content readiness flags (boolean only), asset-workflow status, finalization status, preset, custom property. Pass deliverableIds to look up specific deliverables by ID (e.g. from detail page context). When any groupBy* param is set, returns aggregation buckets instead of a list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true — do not try to derive workspace names from list results.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "deliverableIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Editorial Deliverable GUIDs — look up specific deliverables by ID. JSON array or single ID. Use when navigating from an Editorial Deliverable detail page."
        },
        {
          "name": "deliverableCode",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable code, e.g. 'ED151009CF'. Exact match."
        },
        {
          "name": "deliverableName",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable name (free-text label set by user, e.g. 'AEM Deliverable 2'). Exact match."
        },
        {
          "name": "deliverableOutputName",
          "type": "string",
          "required": false,
          "description": "Output name attached to the deliverable (the customer-facing/publishing label, distinct from internal deliverable name — e.g. 'D 20230627 3'). Exact match."
        },
        {
          "name": "deliverableStatus",
          "type": "string",
          "required": false,
          "description": "Deliverable lifecycle status: Backlog, Todo, InProgress, Done."
        },
        {
          "name": "deliverableStatusId",
          "type": "int?",
          "required": false,
          "description": "Deliverable status as raw integer ID (1000 Backlog, 2000 Todo, 3000 InProgress, 9000 Done). Prefer deliverableStatus (name). Takes precedence over deliverableStatus when both supplied."
        },
        {
          "name": "currentStep",
          "type": "string",
          "required": false,
          "description": "Workflow step the deliverable is currently at. Canonical EnumStep names: Photography (a.k.a. Capture), FinalSelection, ExternalPostProduction (a.k.a. External Post), ExternalPostProductionQc, InternalPostProduction (a.k.a. Internal Post), InternalPostProductionQc, PostReview, AssetDelivery. Resolved via StepNames.FromName."
        },
        {
          "name": "currentStepId",
          "type": "int?",
          "required": false,
          "description": "Workflow step as raw integer stepId. Prefer currentStep (name). Takes precedence over currentStep when both supplied."
        },
        {
          "name": "currentStepStatus",
          "type": "string",
          "required": false,
          "description": "Status WITHIN the current step: Backlog, Todo, InProgress, Done, Failed (7000), Rejected (5000), QCRejected (5100), Bypassed (8000)."
        },
        {
          "name": "currentStepStatusId",
          "type": "int?",
          "required": false,
          "description": "Step status as raw integer ID (1000 Backlog / 2000 Todo / 3000 InProgress / 9000 Done / 7000 Failed / 5000 Rejected / 5100 QCRejected / 8000 Bypassed). Prefer currentStepStatus (name). Takes precedence over currentStepStatus when both supplied."
        },
        {
          "name": "deliverableDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest deliverable deadline. ISO date or epoch ms."
        },
        {
          "name": "deliverableDueDateTo",
          "type": "string",
          "required": false,
          "description": "Latest deliverable deadline. ISO date or epoch ms. Exclusive (`lt`). For overdue: set to now and combine with deliverableStatus != Done."
        },
        {
          "name": "ownerName",
          "type": "string",
          "required": false,
          "description": "Deliverable Owner full name (or GUID) — the user who owns this deliverable. No 'me'/currentUser resolver — caller must pass the explicit name."
        },
        {
          "name": "hasNoOwner",
          "type": "bool",
          "required": false,
          "description": "When true, matches deliverables where ownerId is empty/missing (no owner assigned)."
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client display name (or GUID) — the customer that owns this deliverable's project."
        },
        {
          "name": "projectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter deliverables to a set of Editorial Project GUIDs. Single project: pass [guid]. Multiple: [guid1, guid2, ...]. Populate by upstream-calling query_editorial_project (by code/name/campaign/PM/custom-property) and reading projectId from each hit. Filters via top-level denormalized projectId field on the deliverable doc — no has_parent join needed. Accepts a JSON array or a JSON-encoded string array."
        },
        {
          "name": "projectName",
          "type": "string",
          "required": false,
          "description": "Parent project name (campaign/initiative name set by user). Exact match. Filters deliverables whose parent Editorial Project has this name."
        },
        {
          "name": "projectCode",
          "type": "string",
          "required": false,
          "description": "Parent project code, e.g. 'EPJ107192CF'. Exact match."
        },
        {
          "name": "projectCampaign",
          "type": "string",
          "required": false,
          "description": "Marketing campaign reference attached to the parent project (free-text). Exact match."
        },
        {
          "name": "projectStatus",
          "type": "string",
          "required": false,
          "description": "Parent project lifecycle status: Backlog, Todo, InProgress, Done."
        },
        {
          "name": "projectStatusId",
          "type": "int?",
          "required": false,
          "description": "Parent project status as raw integer ID (1000 Backlog, 2000 Todo, 3000 InProgress, 9000 Done). Prefer projectStatus (name). Takes precedence over projectStatus when both supplied."
        },
        {
          "name": "projectDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest parent-project deadline. ISO date or epoch ms."
        },
        {
          "name": "projectDueDateTo",
          "type": "string",
          "required": false,
          "description": "Latest parent-project deadline. ISO date or epoch ms. Exclusive (`lt`)."
        },
        {
          "name": "projectManagerName",
          "type": "string",
          "required": false,
          "description": "Parent project's Project Manager full name (or GUID). No 'me'/currentUser resolver — caller must pass the explicit name."
        },
        {
          "name": "projectCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Parent project custom property name (customer-defined field on the EditorialProject, e.g. 'season') or its GUID. Pair with projectCustomPropertyValue. Distinct from deliverableCustomPropertyName, which lives on the Deliverable doc itself."
        },
        {
          "name": "projectCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for projectCustomPropertyName. Required when projectCustomPropertyName is set. Exact match, lowercased."
        },
        {
          "name": "productsAdded",
          "type": "string",
          "required": false,
          "description": "Whether products have been added to the deliverable. Added (productsCount >= 1), NotAdded (productsCount < 1), All (no filter)."
        },
        {
          "name": "samplesAdded",
          "type": "string",
          "required": false,
          "description": "Sample-assignment flag on the deliverable (boolean-level only — does NOT return sample details). Added = at least one sample assigned (assignedSamplesCount >= 1). NotAdded = no samples assigned. Available = at least one sample physically checked in (checkedInSamplesCount >= 1). NotAvailable = no checked-in samples. All = no filter. To get the actual list of samples for a project, use query_sample with editorialProjectIds instead."
        },
        {
          "name": "selectsAdded",
          "type": "string",
          "required": false,
          "description": "Whether assets have been selected for the deliverable. Added (selectedAssetsCount >= 1), NotAdded (selectedAssetsCount < 1), All."
        },
        {
          "name": "moodBoardImagesAdded",
          "type": "string",
          "required": false,
          "description": "Whether the Creative Brief has mood-board images. Added (moodboardImagesCount >= 1), NotAdded, All. Counts only — actual images not in ES."
        },
        {
          "name": "directionTextAdded",
          "type": "string",
          "required": false,
          "description": "Whether the Creative Brief has direction text. Added = field exists; NotAdded = field absent. Content itself is NOT in ES — only the existence check."
        },
        {
          "name": "documentsAdded",
          "type": "string",
          "required": false,
          "description": "Whether the Creative Brief has documents. Added (creativeBriefDocumentsCount >= 1), NotAdded, All. Counts only."
        },
        {
          "name": "mockupsAdded",
          "type": "string",
          "required": false,
          "description": "Whether the Creative Brief has mock-ups. Added (creativeBriefMockupsCount >= 1), NotAdded, All. Counts only."
        },
        {
          "name": "assetWorkflowStatusIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Asset Workflow Status filter (multi-select). Status IDs of asset-side workflow: Todo (2000), InProgress (3000), Done (9000). Matches deliverables whose denormalized assetWorkflowStatusIds[] array contains any of the supplied IDs. Accepts a JSON array of integers or a JSON-encoded string array (ES coerces string→int)."
        },
        {
          "name": "finalizationStatus",
          "type": "string",
          "required": false,
          "description": "Finalization workflow status: Todo, InProgress, Done."
        },
        {
          "name": "finalizationStatusId",
          "type": "int?",
          "required": false,
          "description": "Finalization status as raw integer ID. Prefer finalizationStatus (name). Takes precedence when both supplied."
        },
        {
          "name": "presetName",
          "type": "string",
          "required": false,
          "description": "Editorial Preset name (or GUID) attached to the deliverable. Resolved to presetId via masterDataCache.GetEditorialPresetsAsync."
        },
        {
          "name": "deliverableCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable custom property name (customer-defined field, e.g. 'collection', 'season') or its GUID. Pair with deliverableCustomPropertyValue to filter, or with groupByCustomProperty=true to aggregate."
        },
        {
          "name": "deliverableCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for deliverableCustomPropertyName. Required when deliverableCustomPropertyName is set in filter mode. Exact match, lowercased."
        },
        {
          "name": "groupByCurrentStep",
          "type": "bool",
          "required": false,
          "description": "Group by current workflow step — aggregation mode. Returns counts by step name (resolved via StepNames.FromId)."
        },
        {
          "name": "groupByCurrentStepStatus",
          "type": "bool",
          "required": false,
          "description": "Group by current step status — aggregation mode. Returns counts by status name (Backlog/Todo/InProgress/Done/Failed/Rejected, resolved via StepStatusNames.FromId)."
        },
        {
          "name": "groupByDeliverableStatus",
          "type": "bool",
          "required": false,
          "description": "Group by deliverable lifecycle status — aggregation mode. Returns counts by status name (Backlog/Todo/InProgress/Done)."
        },
        {
          "name": "groupByAssetWorkflowStatus",
          "type": "bool",
          "required": false,
          "description": "Group by asset workflow status — aggregation mode. Note: assetWorkflowStatusIds is an array field, so a deliverable with multiple values increments multiple buckets."
        },
        {
          "name": "groupByProject",
          "type": "bool",
          "required": false,
          "description": "Group by parent Editorial Project — aggregation mode. Returns counts by projectId (raw GUID — caller can resolve to projectCode/projectName via query_editorial_project if names needed)."
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. MUST be set to true for any 'per workspace', 'per client', 'by workspace', 'breakdown by client' question. Returns counts by workspace name. Do NOT attempt to group by workspace manually from list results — use this param to get accurate server-side aggregation."
        },
        {
          "name": "groupByOwner",
          "type": "bool",
          "required": false,
          "description": "Group by Deliverable Owner — aggregation mode. Returns counts by owner full name (batch-resolved from ownerId)."
        },
        {
          "name": "groupByCustomProperty",
          "type": "bool",
          "required": false,
          "description": "Group by the value of deliverableCustomPropertyName — aggregation mode. Requires deliverableCustomPropertyName to be set."
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Time-bucket aggregation: hour, day, week, or month. Pair with groupByPeriodField to choose which date drives the histogram."
        },
        {
          "name": "groupByPeriodField",
          "type": "string",
          "required": false,
          "description": "Date field driving the time-bucket histogram. Default updatedDatetimeUtc. Other options: deliverableDueDateUtc (deadlines), createdDateTimeUtc (creation pace), finishedDateTimeUtc (completion pace)."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results in list mode (ignored in aggregation). Default 50. Use 0 for total-count-only."
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "0-based page index for list mode (ignored in aggregation). Default 0."
        }
      ],
      "exampleInput": "{ \"projectName\": \"Back-to-school 2026\", \"currentStepStatus\": \"InProgress\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 9, \"hits\": [ { \"deliverableId\": \"c6391d01-...\", \"deliverableName\": \"AEM Deliverable 1\", \"currentStep\": \"InternalPostProduction\", \"deliverableStatus\": \"InProgress\" } ] }",
      "knownLimitations": "Per-output unit within an Editorial Project. For project-level rollups use query_editorial_project; for work-unit progress use query_editorial_production."
    },
    {
      "name": "query_editorial_production",
      "title": "Query Editorial Production",
      "module": "Editorial",
      "description": "Search Editorial Productions (also called Editorial work units) — the central WorkUnit-doc row that tracks an Editorial Deliverable's content through the workflow (contentCreationTypeId=2). Use to answer: which work units exist for a project/deliverable, what step a production is at, who's shooting it, how many at each status/step, throughput by week, work unit counts by project/workspace/photographer. Filter by project (name/code/due-date/custom-property), deliverable (name/code/due-date/custom-property), product code, sample (code/name/custom-property), workflow type ('Asset Workflow' / 'Finalization'), workflow status ('To Do'/'In Progress'/'Done'), current step (Capture/Final Selection/External Post/External Post QC/Internal Post/Internal Post QC/Post Review/Asset Delivery), current step status ('Backlog'/'To Do'/'Rejected'/'In Progress'/'Failed'/'Done'), photographer/videographer/stylist/hair-and-makeup/model/art-director/assistant (Editorial team-on-set), External Post Vendor, External Post QC / Internal Post / Internal Post QC / Post Review assignees, workspace, media type (Image/Video — feature-gated), round of review, asset-delivery-finished date range, Selection/Post-Review collection statuses. When any groupBy* param is set, returns aggregation buckets instead of a list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true — do not try to derive workspace names from list results. Editorial only — for E-comm production rows use query_ecomm_production. For the parent project itself use query_editorial_project; for deliverable-level filtering use query_editorial_deliverable.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "projectName",
          "type": "string",
          "required": false,
          "description": "Editorial Project name (campaign/initiative label set by user, e.g. 'DN Project'). Exact match. Filters work units whose denormalized parent project has this name."
        },
        {
          "name": "projectCode",
          "type": "string",
          "required": false,
          "description": "Editorial Project code (e.g. 'EPJ101577CF'). Exact match."
        },
        {
          "name": "projectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter work units to a set of Editorial Project GUIDs. Single project: pass [guid]. Multiple: [guid1, guid2, ...]. Drill-down: populate by upstream-calling query_editorial_project (by code/name/campaign/PM/custom-property) and reading projectId from each hit. Filters via top-level denormalized editorialProjectId field on the work unit doc — no has_parent join. Accepts a JSON array or a JSON-encoded string array."
        },
        {
          "name": "projectDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest project deadline. ISO date or epoch ms. Pair with projectDueDateTo."
        },
        {
          "name": "projectDueDateTo",
          "type": "string",
          "required": false,
          "description": "Latest project deadline. ISO date or epoch ms. Inclusive (`lte`)."
        },
        {
          "name": "projectCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Editorial Project custom property name (customer-defined field on the parent project, e.g. 'season', 'region') or its GUID. Pair with projectCustomPropertyValue. Resolved via masterDataCache.GetEditorialProjectCustomPropertiesAsync (objectType=100). Distinct from deliverableCustomPropertyName, which lives on the Deliverable denormalization."
        },
        {
          "name": "projectCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for projectCustomPropertyName. Required when projectCustomPropertyName is set in filter mode. Exact match, lowercased."
        },
        {
          "name": "deliverableName",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable name (free-text label set by user, e.g. 'AEM Deliverable 3'). Exact match. Matches against denormalized editorialDeliverableOfFinalizeWorkflow.deliverableName on the work unit doc."
        },
        {
          "name": "deliverableCode",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable code (e.g. 'ED148161CF'). Exact match."
        },
        {
          "name": "deliverableIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter work units to a set of Editorial Deliverable GUIDs. Drill-down: populate by upstream-calling query_editorial_deliverable. Matches via the top-level denormalized editorialDeliverableIds[] array field on the work unit doc (a finalization workflow may target multiple deliverables). Accepts a JSON array or a JSON-encoded string array."
        },
        {
          "name": "deliverableDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest deliverable deadline. ISO date or epoch ms. For overdue: leave From null and combine with editorialWorkflowStatusId not in [Done]."
        },
        {
          "name": "deliverableDueDateTo",
          "type": "string",
          "required": false,
          "description": "Latest deliverable deadline. ISO date or epoch ms. Inclusive (`lte`)."
        },
        {
          "name": "deliverableCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Editorial Deliverable custom property name (customer-defined field on the deliverable, e.g. 'channel', 'collection') or its GUID. Pair with deliverableCustomPropertyValue. Resolved via masterDataCache.GetEditorialDeliverableCustomPropertiesAsync (objectType=101)."
        },
        {
          "name": "deliverableCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for deliverableCustomPropertyName. Required when deliverableCustomPropertyName is set in filter mode. Exact match, lowercased."
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code on the work unit (exact match, lowercased). Filters via nested query on the products array (each work unit may carry multiple products)."
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name on the work unit (exact match, lowercased). Same nested-array semantics as productCode."
        },
        {
          "name": "workflowType",
          "type": "string",
          "required": false,
          "description": "Editorial workflow type: 'Asset Workflow' (1 — upstream content-creation phase) or 'Finalization' (2 — targets a specific deliverable in the collation/finalize phase). Filter UI default is 'All' (no filter). Resolved by name to editorialWorkflowTypeId. Use workflowTypeId for raw integer."
        },
        {
          "name": "workflowTypeId",
          "type": "int?",
          "required": false,
          "description": "Editorial workflow type as raw integer ID (1 = Asset Workflow, 2 = Finalization). Prefer workflowType (name). Takes precedence over workflowType when both supplied."
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "Editorial Production (work unit) lifecycle status: 'To Do' (2000), 'In Progress' (3000), 'Done' (9000)."
        },
        {
          "name": "statusId",
          "type": "int?",
          "required": false,
          "description": "Work unit status as raw integer (2000 = To Do, 3000 = In Progress, 9000 = Done). Prefer status (name). Takes precedence when both supplied."
        },
        {
          "name": "currentStep",
          "type": "string",
          "required": false,
          "description": "Workflow step the work unit is currently at. Canonical EnumStep names: Photography (a.k.a. Capture), FinalSelection, ExternalPostProduction (a.k.a. External Post), ExternalPostProductionQc, InternalPostProduction (a.k.a. Internal Post), InternalPostProductionQc, PostReview, AssetDelivery. Resolved via StepNames.FromName."
        },
        {
          "name": "currentStepId",
          "type": "int?",
          "required": false,
          "description": "Workflow step as raw integer stepId. Prefer currentStep (name). Takes precedence when both supplied."
        },
        {
          "name": "currentStepStatus",
          "type": "string",
          "required": false,
          "description": "Status WITHIN the current step. Editorial Production filter exposes: 'Backlog' (1000), 'To Do' (2000), 'Rejected' (5000), 'In Progress' (3000), 'Failed' (7000), 'Done' (9000). Use this for 'blockers' / 'rejected work' queries."
        },
        {
          "name": "currentStepStatusId",
          "type": "int?",
          "required": false,
          "description": "Step status as raw integer (1000 Backlog / 2000 To Do / 3000 In Progress / 5000 Rejected / 7000 Failed / 9000 Done). Prefer currentStepStatus (name)."
        },
        {
          "name": "currentRound",
          "type": "int?",
          "required": false,
          "description": "Round-of-review number the work unit is currently on (1, 2, 3, ...). Filters via top-level currentRound field on the work unit doc. Available only when POST_REVIEW + ROUND_OF_REVIEW subscription features are enabled."
        },
        {
          "name": "photographer",
          "type": "string",
          "required": false,
          "description": "Photographer who shot/captured/photographed the work unit. Filter by photographer full name or user GUID. Use for 'shot by Anna', 'photos by John', 'work units captured by ...'."
        },
        {
          "name": "videographer",
          "type": "string",
          "required": false,
          "description": "Videographer who recorded/filmed the work unit. Filter by full name or user GUID. Available only when ED_VIDEO or VIDEO subscription features are enabled."
        },
        {
          "name": "stylist",
          "type": "string",
          "required": false,
          "description": "Stylist who styled/dressed the work unit. Filter by full name or user GUID."
        },
        {
          "name": "model",
          "type": "string",
          "required": false,
          "description": "Model who appeared/posed in the work unit. Filter by full name or user GUID."
        },
        {
          "name": "artDirector",
          "type": "string",
          "required": false,
          "description": "Art Director who directed the work unit. Filter by full name or user GUID."
        },
        {
          "name": "hairAndMakeup",
          "type": "string",
          "required": false,
          "description": "Hair & Makeup artist on the work unit. Filter by full name or user GUID."
        },
        {
          "name": "assistant",
          "type": "string",
          "required": false,
          "description": "Assistant on set. Filter by full name or user GUID."
        },
        {
          "name": "externalPostVendor",
          "type": "string",
          "required": false,
          "description": "External Post Vendor assigned to the External Post Production step (only for the canonical ExternalPostProduction step). Resolved via masterDataCache.GetPostProductionVendorsAsync. Filter by vendor display name or GUID."
        },
        {
          "name": "externalPostQcAssignee",
          "type": "string",
          "required": false,
          "description": "Person assigned to the External Post QC step (canonical ExternalPostProductionQc). Filter by full name or user GUID."
        },
        {
          "name": "internalPostAssignee",
          "type": "string",
          "required": false,
          "description": "Retoucher / person assigned to the Internal Post Production step. Use for 'retouched by ...'. Filter by full name or user GUID."
        },
        {
          "name": "internalPostQcAssignee",
          "type": "string",
          "required": false,
          "description": "Person assigned to the Internal Post QC step. Filter by full name or user GUID."
        },
        {
          "name": "postReviewAssignee",
          "type": "string",
          "required": false,
          "description": "Stakeholder assigned to a Post Review step. Filter by full name or user GUID. Multiple Post Review steps may exist (one per round); filter targets all post-review-category step IDs (PostReview, PostReviewL1..L5). Available only when POST_REVIEW subscription feature is enabled."
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client display name (or GUID). Resolved via masterDataCache.GetWorkspacesAsync. Filters via top-level clientId on the work unit doc."
        },
        {
          "name": "sampleCode",
          "type": "string",
          "required": false,
          "description": "Sample code on the work unit (exact match, lowercased). Filters via denormalized samples.sampleCodes[] array on the work unit doc."
        },
        {
          "name": "sampleName",
          "type": "string",
          "required": false,
          "description": "Sample name on the work unit (exact match, lowercased)."
        },
        {
          "name": "sampleCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Sample custom property name (e.g. 'fabric', 'colorVariant') or GUID. Pair with sampleCustomPropertyValue. Resolved via masterDataCache.GetSampleCustomPropertiesAsync (objectType=3)."
        },
        {
          "name": "sampleCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for sampleCustomPropertyName. Required when sampleCustomPropertyName is set in filter mode. Exact match, lowercased."
        },
        {
          "name": "mediaType",
          "type": "string",
          "required": false,
          "description": "Media type bucket: 'Image' or 'Video'. Video maps to shootingTypeCategoryId [30 video, 90 generative]; Image is the inverse (must_not [30, 90, 70 copyWriting]). Available only when ED_VIDEO subscription feature is enabled."
        },
        {
          "name": "assetDeliveryFinishedDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest asset-delivery-finished date for the work unit (when assets were delivered to client — the canonical 'completion' moment for an Editorial Production). ISO date or epoch ms. Pair with assetDeliveryFinishedDateTo."
        },
        {
          "name": "assetDeliveryFinishedDateTo",
          "type": "string",
          "required": false,
          "description": "Latest asset-delivery-finished date. ISO date or epoch ms. Inclusive (`lte`)."
        },
        {
          "name": "workUnitIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter to a specific set of Editorial Production (work unit) GUIDs. Drill-down: pass the IDs returned from a previous query_editorial_production call when fetching specific rows. Accepts a JSON array or a JSON-encoded string array."
        },
        {
          "name": "planningSessionIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Filter editorial productions (work units) by Planning Session GUIDs. Drill-down: pass sessionId(s) returned from query_planning. Multi-value — matches if the production's planningSessionId is in the list. Accepts a JSON array or a JSON-encoded string array."
        },
        {
          "name": "collectionStatusIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Selection / Post-Review collection statuses (multi-select) attached to child tasks. Status IDs: 2000 Todo, 2010 Pending, 2100 Submitted, 2200 PartiallySubmitted, 3000 InProgress, 4000 Approved, 5000 Rejected, 9000 Done. Filter compiles to has_child(task) → nested(collections.collectionStatusId). Available only when SHARE_FOR_EXT_REVIEW or SELECTION_COLLECTIONS subscription features are enabled. Accepts a JSON array of integers or a JSON-encoded string array."
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by work unit lifecycle status — aggregation mode. Returns counts by status name (Todo/InProgress/Done)."
        },
        {
          "name": "groupByCurrentStep",
          "type": "bool",
          "required": false,
          "description": "Group by current workflow step — aggregation mode. Returns counts by step name (resolved via StepNames.FromId)."
        },
        {
          "name": "groupByCurrentStepStatus",
          "type": "bool",
          "required": false,
          "description": "Group by current step status — aggregation mode."
        },
        {
          "name": "groupByWorkflowType",
          "type": "bool",
          "required": false,
          "description": "Group by editorial workflow type — aggregation mode. Counts split between AssetWorkflow (1) and FinalizationWorkflow (2)."
        },
        {
          "name": "groupByProject",
          "type": "bool",
          "required": false,
          "description": "Group by parent Editorial Project — aggregation mode. Returns counts by editorialProjectId (raw GUID)."
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. MUST be set to true for any 'per workspace', 'per client', 'by workspace', 'breakdown by client' question. Returns counts by workspace name. Do NOT attempt to group by workspace manually from list results — use this param to get accurate server-side aggregation."
        },
        {
          "name": "groupByPhotographer",
          "type": "bool",
          "required": false,
          "description": "Group by photographer — aggregation mode. Counts by photographer full name (batch-resolved)."
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Time-bucket aggregation: hour, day, week, or month. Pair with groupByPeriodField to choose which date drives the histogram."
        },
        {
          "name": "groupByPeriodField",
          "type": "string",
          "required": false,
          "description": "Date field driving the time-bucket histogram. Default assetDeliveryFinishedDateTimeUtc (delivery completion pace). Alternative: deliverableDueDateUtc (workload by deadline — resolves to editorialDeliverableOfFinalizeWorkflow.deliverableDueDateUtc)."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results in list mode (ignored in aggregation). Default 50. Use 0 for total-count-only."
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "0-based page index for list mode (ignored in aggregation). Default 0."
        }
      ],
      "exampleInput": "{ \"projectName\": \"Back-to-school 2026\", \"currentStep\": \"InternalPostProduction\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 14, \"hits\": [ { \"workUnitId\": \"e2...\", \"deliverableName\": \"AEM Deliverable 1\", \"currentStep\": \"InternalPostProduction\", \"status\": \"InProgress\" } ] }",
      "knownLimitations": "Editorial work-unit (content moving through the workflow). For the delivery row itself use query_editorial_deliverable."
    },
    {
      "name": "query_editorial_project",
      "title": "Query Editorial Project",
      "module": "Editorial",
      "description": "Search Editorial Projects — campaign/initiative-level container in the Editorial module (e.g. 'Back-to-school 2026'). Use to answer: which projects exist, project status, due date, who manages it, project counts by status/workspace/PM, how many projects per workspace/client. Filter by code, name, campaign, status, project manager, workspace, due/created date, deliverables count, custom property. Pass projectIds to look up specific projects by ID (e.g. from detail page context). When any groupBy* param is set, returns aggregation buckets instead of a list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true — do not try to derive workspace names from list results.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "projectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Editorial Project GUIDs — look up specific projects by ID. JSON array or single ID. Use when navigating from an Editorial Project detail page."
        },
        {
          "name": "projectCode",
          "type": "string",
          "required": false,
          "description": "Editorial Project code, e.g. 'EPJ101577CF'. Exact match."
        },
        {
          "name": "projectName",
          "type": "string",
          "required": false,
          "description": "Editorial Project name (campaign/initiative name set by user). Exact match."
        },
        {
          "name": "projectCampaign",
          "type": "string",
          "required": false,
          "description": "Marketing campaign reference attached to the project (free-text). Exact match."
        },
        {
          "name": "projectStatus",
          "type": "string",
          "required": false,
          "description": "Project lifecycle status: Backlog, Todo, InProgress, Done."
        },
        {
          "name": "projectStatusId",
          "type": "int?",
          "required": false,
          "description": "Project status as raw integer ID (1000 Backlog, 2000 Todo, 3000 InProgress, 9000 Done). Prefer projectStatus (name). Takes precedence over projectStatus when both supplied."
        },
        {
          "name": "projectDueDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest project deadline. ISO date or epoch ms."
        },
        {
          "name": "projectDueDateTo",
          "type": "string",
          "required": false,
          "description": "Latest project deadline. ISO date or epoch ms. Exclusive (`lt`). For overdue: set to now and combine with projectStatus != Done."
        },
        {
          "name": "projectManagerName",
          "type": "string",
          "required": false,
          "description": "Project Manager full name (or GUID). No 'me'/currentUser resolver — caller must pass the explicit name."
        },
        {
          "name": "hasNoProjectManager",
          "type": "bool",
          "required": false,
          "description": "When true, matches projects where projectManagerIds is empty/missing (unassigned PM)."
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client display name (or GUID) — the customer that owns the project."
        },
        {
          "name": "projectCustomPropertyName",
          "type": "string",
          "required": false,
          "description": "Editorial Project custom property name (customer-defined field, e.g. 'season', 'collection') or its GUID. Pair with projectCustomPropertyValue to filter, or with groupByCustomProperty=true to aggregate."
        },
        {
          "name": "projectCustomPropertyValue",
          "type": "string",
          "required": false,
          "description": "Value to match for projectCustomPropertyName. Required when projectCustomPropertyName is set for filter mode. Exact match, lowercased."
        },
        {
          "name": "projectCreatedDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest project-created date. ISO or epoch ms. Pair with groupByPeriod for time-series."
        },
        {
          "name": "projectCreatedDateTo",
          "type": "string",
          "required": false,
          "description": "Latest project-created date. ISO or epoch ms. Exclusive (`lt`)."
        },
        {
          "name": "deliverablesCountMin",
          "type": "int?",
          "required": false,
          "description": "Minimum deliverable count."
        },
        {
          "name": "deliverablesCountMax",
          "type": "int?",
          "required": false,
          "description": "Maximum deliverable count. Set to 0 for projects with no deliverables yet."
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by project status — aggregation mode. Returns counts by status name."
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. MUST be set to true for any 'per workspace', 'per client', 'by workspace', 'breakdown by client' question. Returns counts by workspace name. Do NOT attempt to group by workspace manually from list results — use this param to get accurate server-side aggregation."
        },
        {
          "name": "groupByProjectManager",
          "type": "bool",
          "required": false,
          "description": "Group by Project Manager — aggregation mode. Returns counts by PM name. Note: a project with 2 PMs increments both buckets (array-field terms agg)."
        },
        {
          "name": "groupByCustomProperty",
          "type": "bool",
          "required": false,
          "description": "Group by the value of projectCustomPropertyName — aggregation mode. Requires projectCustomPropertyName to be set."
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Time-bucket aggregation: hour, day, week, or month. Pair with groupByPeriodField to choose which date drives the histogram."
        },
        {
          "name": "groupByPeriodField",
          "type": "string",
          "required": false,
          "description": "Date field driving the time-bucket histogram. Default createdDateTimeUtc. Other options: projectDueDateUtc (deadlines), finishedDateTimeUtc (completion pace), updatedDatetimeUtc (recently touched)."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results in list mode (ignored in aggregation). Default 50. Use 0 for total-count-only."
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "0-based page index for list mode (ignored in aggregation). Default 0."
        }
      ],
      "exampleInput": "{ \"projectStatus\": \"InProgress\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 6, \"hits\": [ { \"projectId\": \"9d83efa3-...\", \"projectCode\": \"ED-2026-01\", \"projectName\": \"Back-to-school 2026\", \"projectStatus\": \"InProgress\" } ] }",
      "knownLimitations": "Campaign-level container only — for per-output rows use query_editorial_deliverable."
    },
    {
      "name": "query_planning",
      "title": "Query Planning",
      "module": "Planning",
      "description": "**Custom Resource trigger** → if user prompt names a production support role NOT in the standard productionTeam (Photographer/Stylist/Model/Art Director/Hair&Makeup/Assistant/Videographer/DigitalProcessing) → call this tool with `includeCustomResources=true`. Common triggers: 'cater', 'lunch', 'runner', 'driver', 'PA', 'DIT', 'dog handler', 'medic' (and any similar free-form role). Custom Resource labels are FREE-FORM text studios type themselves → semantic-match them yourself from `customResourceLabelsAvailable[]` + `customResources[]` in the response. **Tool-first, ask-later**: call the tool, then if `customResourceLabelsAvailable=[]` ask user; otherwise semantic-match from availableLabels. **Trailing-junk / cut-off tolerance**: if user prompt has trailing backslash (`\\`), ellipsis (`...`), question marks (`??`), or appears truncated mid-word, IGNORE the noise and treat the readable trigger word as the query. Do NOT respond with 'your message looks cut off' — call the tool with the cleaned query. Examples: 'Who is today's Cater' → query for cater. 'Lunch...' → query for lunch. 'DIT??' → query for DIT. **Likely-misread tolerance**: if a trigger word could be misread as a proper noun (e.g. 'Cater' → 'Carter', 'DIT' → a person's initials), DEFAULT to the production-role interpretation (this tool is in a production planning context, not a general-knowledge chat). Don't suggest sports/celebrity readings of trigger words. **Default 'today' to ISO date**: when user says 'today', 'hôm nay', 'this morning', etc., automatically set `dateFrom`=`dateTo`=current date in `YYYY-MM-DD` (server timezone UTC). Same for 'tomorrow' (+1 day), 'yesterday' (-1 day), 'this week' (Mon-Sun current week). Don't ask the user to specify the date when they used a relative phrase. Search planning sessions — scheduled production shoots with team, time slots, and progress. Use this to answer: what's planned, when (ISO 8601 UTC), where, who's on set, sample check-in status, photography progress, alerts. **Who-is-working queries**: when user asks 'who is working this week', 'which talent has sessions today', 'list photographers on sessions next month' → set `groupByTeamMember=true` with `dateFrom`/`dateTo`. Response flips to person-centric: each entry shows the team member with their sessions. This is the primary way to answer talent-on-session questions when the studio does not use the Resource Booking add-on. Filter by team member (any role) or specific role (photographer, stylist, model, art director, assistant, videographer, hair & makeup). Also filter by sessionCode (e.g. 'SES137123CF'), sessionName (partial), location, production type, check-in/alert/photography status, date range. Filter by associated job (jobCode/jobName, or `job` ambiguous = code OR name) and product attributes (productCode/productName, or `product` ambiguous = code OR name, plus brand, category, color, styleCode) — exact match, case-insensitive, matches if ANY of the session's jobs/products satisfies the filter. Editorial workflow: filter by associated project (projectCode/projectName, or `project` ambiguous = code OR name, plus campaign) and deliverable (deliverableCode/deliverableName, or `deliverable` ambiguous = code OR name, plus deliverableOutputName) — exact match, case-insensitive, matches if ANY of the session's projects/deliverables satisfies the filter. Use `user` to find a session where a given user is on the production team regardless of skill/role (V2 ProductionTeams). Returns: session `id` (UUID), sessionName, sessionCode, time slots (ISO 8601 UTC), full production team (resolved names), check-in status, photography progress, job codes, product codes. When groupBy params set, returns counts/aggregations instead of list. **Session lookup param choice — strict per user phrasing:**   • User says 'session code SES137123CF' / 'code SES…' → `sessionCode` only.   • User says 'session named Nike Spring' / 'the Spring shoot' → `sessionName` only.   • User says 'session SES137123CF' / 'session Nike Spring' WITHOUT specifying code-vs-name → `session` (ambiguous param, matches code OR name OR UUID). Don't pass multiple session params at once — pick the one that matches user intent. **Chain with query_talent_crew_schedule**: when the user asks about bookings/holds for a specific session, FIRST call this tool → take response's `id` and `timeSlots` → pass to `query_talent_crew_schedule(sessionId=<id>, dateFrom=<min start>, dateTo=<max end>, ...)`. **Custom Resource queries** (cater / catering / lunch / food / runner / driver / transport / PA / production assistant / makeup artist (one-off) / any role NOT in standard productionTeam): set `includeCustomResources=true`. Custom resources are free-form (label, name) pairs attached inline to a session via the FE 'CUSTOM RESOURCE' panel → free-form means studios type whatever they want ('cater', 'Cater', 'catering', 'lunch', 'food service'). The tool returns ALL custom resources on the page and Claude does the semantic match (no server-side filter — too many false-negatives on free-form text like 'Caster' vs 'cater'). **Semantic match rule** (CRITICAL for custom resources): the response includes `customResourceLabelsAvailable: string[]` → distinct labels seen on the page, plus `customResources[{label,name}]` per session. Scan `customResourceLabelsAvailable` for labels **semantically similar** to the user's word, then read the matching `customResources` entries. DO NOT reply 'not found' until you've checked every label. Examples: 'cater' → look for 'catering', 'lunch', 'meal', 'food service', 'snack runner'; 'driver' → 'transport', 'shuttle', 'logistics'; 'PA' → 'production assistant', 'runner', 'gopher'. Tell the user which labels you considered (e.g. 'No exact 'cater' label, but 'catering' is set to John Smith → likely the same role.'). **Custom resource has NO booking status** (no Confirmed/Hold) → it's session-inline data, not a resource booking. Don't mention status pills for these rows.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "sessionName",
          "type": "string",
          "required": false,
          "description": "Session name — wildcard contains, case-insensitive (e.g. 'Nike Spring' matches 'Nike Spring Shoot 2026'). Use when the user explicitly says the session NAME."
        },
        {
          "name": "sessionCode",
          "type": "string",
          "required": false,
          "description": "Session code — exact match (e.g. 'SES137123CF', case-insensitive). Use when the user explicitly says the session CODE. Response's `id` is the session UUID needed by query_talent_crew_schedule."
        },
        {
          "name": "session",
          "type": "string",
          "required": false,
          "description": "Session identifier when the user said something like 'session X' WITHOUT specifying it's a name or code. Server matches X against EITHER sessionCode OR sessionName (wildcard). Don't use this if the user explicitly called it a code or a name — pass `sessionCode` or `sessionName` instead."
        },
        {
          "name": "dateFrom",
          "type": "string",
          "required": false,
          "description": "Time slot start date from (ISO or epoch ms)"
        },
        {
          "name": "dateTo",
          "type": "string",
          "required": false,
          "description": "Time slot start date to (ISO or epoch ms)"
        },
        {
          "name": "location",
          "type": "string",
          "required": false,
          "description": "Location/set name or ID"
        },
        {
          "name": "productionType",
          "type": "string",
          "required": false,
          "description": "Production type name or ID"
        },
        {
          "name": "teamMember",
          "type": "string",
          "required": false,
          "description": "Team member name or user ID (any role — finds person in any team position)"
        },
        {
          "name": "user",
          "type": "string",
          "required": false,
          "description": "User name or user ID — finds person on the session's V2 ProductionTeams regardless of skill/role. Use this when the caller says 'user X' without specifying a role."
        },
        {
          "name": "photographer",
          "type": "string",
          "required": false,
          "description": "Photographer name or user ID"
        },
        {
          "name": "stylist",
          "type": "string",
          "required": false,
          "description": "Stylist name or user ID"
        },
        {
          "name": "model",
          "type": "string",
          "required": false,
          "description": "Model name or user ID"
        },
        {
          "name": "artDirector",
          "type": "string",
          "required": false,
          "description": "Art director name or user ID"
        },
        {
          "name": "assistant",
          "type": "string",
          "required": false,
          "description": "Assistant name or user ID"
        },
        {
          "name": "videographer",
          "type": "string",
          "required": false,
          "description": "Videographer name or user ID"
        },
        {
          "name": "hairAndMakeup",
          "type": "string",
          "required": false,
          "description": "Hair & makeup artist name or user ID"
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code on the session — exact match, case-insensitive (matches if any of the session's jobs has this code)"
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name on the session — exact match, case-insensitive"
        },
        {
          "name": "job",
          "type": "string",
          "required": false,
          "description": "Job identifier when the caller said 'job X' WITHOUT specifying code-vs-name — matches against jobCode OR jobName."
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code on the session — exact match, case-insensitive (matches if any of the session's products has this code)"
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name on the session — exact match, case-insensitive"
        },
        {
          "name": "product",
          "type": "string",
          "required": false,
          "description": "Product identifier when the caller said 'product X' WITHOUT specifying code-vs-name — matches against productCode OR productName."
        },
        {
          "name": "brand",
          "type": "string",
          "required": false,
          "description": "Product brand on the session — exact match, case-insensitive"
        },
        {
          "name": "category",
          "type": "string",
          "required": false,
          "description": "Product category on the session — exact match, case-insensitive"
        },
        {
          "name": "color",
          "type": "string",
          "required": false,
          "description": "Product color on the session — exact match, case-insensitive"
        },
        {
          "name": "styleCode",
          "type": "string",
          "required": false,
          "description": "Product style code on the session — exact match, case-insensitive"
        },
        {
          "name": "projectCode",
          "type": "string",
          "required": false,
          "description": "Editorial project code on the session — exact match, case-insensitive (matches if any of the session's projects has this code)"
        },
        {
          "name": "projectName",
          "type": "string",
          "required": false,
          "description": "Editorial project name on the session — exact match, case-insensitive"
        },
        {
          "name": "project",
          "type": "string",
          "required": false,
          "description": "Editorial project identifier when the caller said 'project X' WITHOUT specifying code-vs-name — matches against projectCode OR projectName."
        },
        {
          "name": "campaign",
          "type": "string",
          "required": false,
          "description": "Editorial project campaign on the session — exact match, case-insensitive"
        },
        {
          "name": "deliverableCode",
          "type": "string",
          "required": false,
          "description": "Editorial deliverable code on the session — exact match, case-insensitive (matches if any of the session's deliverables has this code)"
        },
        {
          "name": "deliverableName",
          "type": "string",
          "required": false,
          "description": "Editorial deliverable name on the session — exact match, case-insensitive"
        },
        {
          "name": "deliverable",
          "type": "string",
          "required": false,
          "description": "Editorial deliverable identifier when the caller said 'deliverable X' WITHOUT specifying code-vs-name — matches against deliverableCode OR deliverableName."
        },
        {
          "name": "deliverableOutputName",
          "type": "string",
          "required": false,
          "description": "Editorial deliverable output name on the session — exact match, case-insensitive"
        },
        {
          "name": "allCheckedIn",
          "type": "bool?",
          "required": false,
          "description": "Only sessions with all items checked in"
        },
        {
          "name": "hasAlerts",
          "type": "bool?",
          "required": false,
          "description": "Only sessions with alerts"
        },
        {
          "name": "photographyDone",
          "type": "bool?",
          "required": false,
          "description": "Only sessions where photography is done"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client ID"
        },
        {
          "name": "groupByLocation",
          "type": "bool",
          "required": false,
          "description": "Group by location — aggregation mode"
        },
        {
          "name": "groupByProductionType",
          "type": "bool",
          "required": false,
          "description": "Group by production type — aggregation mode"
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Group by period: day, week, month — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. Use for any 'per workspace', 'per client', 'by workspace' question."
        },
        {
          "name": "groupByTeamMember",
          "type": "bool",
          "required": false,
          "description": "Group results by team member instead of by session. Returns `teamMembers[]` — each entry has `userId`, `userName`, `skillName`, `sessionCount`, and `sessions[]` (code, name, timeSlots). Use when the user asks WHO is working in a date range (e.g. 'who is working this week', 'which photographers have sessions next month', 'list all talent on sessions today'). This is the primary tool for talent-on-session queries when the studio does not use the Resource Booking add-on. All other filters (dateFrom, dateTo, photographer, skill roles, location, etc.) still apply — this only changes the response shape from session-centric to person-centric."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        },
        {
          "name": "includeCustomResources",
          "type": "bool",
          "required": false,
          "description": "Include `customResources: [{label, name}]` per session AND `customResourceLabelsAvailable: string[]` (distinct labels seen on this page) in the response. Default false (saves 1 batch round-trip to /sessions/enrich). Pass true when the user prompt mentions a role NOT in the standard productionTeam list (Photographer/Stylist/Model/Art Director/Hair&Makeup/Assistant/Videographer/DigitalProcessing) — e.g. cater, catering, lunch, food, runner, driver, transport, PA, production assistant. Custom resources have NO booking status — they're inline session data, not bookings."
        }
      ],
      "exampleInput": "{ \"dateFrom\": \"2026-06-01\", \"dateTo\": \"2026-06-30\", \"location\": \"Studio A\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 7, \"hits\": [ { \"sessionId\": \"p1...\", \"sessionName\": \"SS26 Day 1\", \"date\": \"2026-06-12\", \"location\": \"Studio A\" } ] }",
      "knownLimitations": "Time slots are stored as epoch ms and returned as ISO 8601. Non-standard production-support roles are only included when includeCustomResources is set."
    },
    {
      "name": "query_sample",
      "title": "Query Sample",
      "module": "Samples",
      "description": "Search samples (physical product items) — track location, check-in/out status, and containers. The only tool that returns actual sample records. Use this to answer: where is a sample, is it checked in, which samples are at a location, which samples are overdue for return, which samples belong to a project, counts by location/status. Samples are linked to editorial projects — use editorialProjectIds to filter samples by project. Check-in status: WaitingForCheckingIn, CheckedIn, CheckedOut. Filter by sample code, barcode, product code, job, location, check-in status, workspace, container, check-in/out dates, editorialProjectIds. Use human-readable names for location (resolved to IDs automatically). When any groupBy* param is set, returns aggregation buckets instead of a list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "sampleCode",
          "type": "string",
          "required": false,
          "description": "Sample code (exact match)."
        },
        {
          "name": "sampleName",
          "type": "string",
          "required": false,
          "description": "Sample name (exact match)."
        },
        {
          "name": "sampleCfBarcode",
          "type": "string",
          "required": false,
          "description": "CF barcode of the sample (exact match). Primary identifier for physical tracking."
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code — find samples belonging to a product."
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code — find samples for products in this job."
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name — find samples for products in this job."
        },
        {
          "name": "productState",
          "type": "string",
          "required": false,
          "description": "Product state: 'Active' (in production) or 'Inactive' (ignored/excluded)."
        },
        {
          "name": "checkInStatus",
          "type": "string",
          "required": false,
          "description": "Check-in status: WaitingForCheckingIn, CheckedIn, CheckedOut."
        },
        {
          "name": "location",
          "type": "string",
          "required": false,
          "description": "Location/sub-location name or ID."
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID."
        },
        {
          "name": "returnDateFrom",
          "type": "string",
          "required": false,
          "description": "Sample return date from (epoch ms or ISO)."
        },
        {
          "name": "returnDateTo",
          "type": "string",
          "required": false,
          "description": "Sample return date to (epoch ms or ISO)."
        },
        {
          "name": "containerCode",
          "type": "string",
          "required": false,
          "description": "Container/box name or ID where the sample is stored."
        },
        {
          "name": "containerType",
          "type": "string",
          "required": false,
          "description": "Container type: 'HangingRail' or 'Box'."
        },
        {
          "name": "checkedInDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest sample check-in date. ISO date or epoch ms."
        },
        {
          "name": "checkedInDateTo",
          "type": "string",
          "required": false,
          "description": "Latest sample check-in date."
        },
        {
          "name": "checkedOutDateFrom",
          "type": "string",
          "required": false,
          "description": "Earliest sample check-out date. ISO date or epoch ms."
        },
        {
          "name": "checkedOutDateTo",
          "type": "string",
          "required": false,
          "description": "Latest sample check-out date."
        },
        {
          "name": "customPropertyName",
          "type": "string",
          "required": false,
          "description": "Custom property name (resolved to GUID) or GUID directly."
        },
        {
          "name": "customPropertyValue",
          "type": "string",
          "required": false,
          "description": "Custom property value to match (exact)."
        },
        {
          "name": "editorialProjectIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Editorial Project IDs — drill-down array from query_editorial_project."
        },
        {
          "name": "groupByCheckInStatus",
          "type": "bool",
          "required": false,
          "description": "Group by check-in status — aggregation mode."
        },
        {
          "name": "groupByLocation",
          "type": "bool",
          "required": false,
          "description": "Group by location — aggregation mode."
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. MUST be set for 'per workspace/client' questions."
        },
        {
          "name": "groupByContainerType",
          "type": "bool",
          "required": false,
          "description": "Group by container type — aggregation mode."
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Time-bucket aggregation: hour, day, week, or month. Pair with groupByPeriodField."
        },
        {
          "name": "groupByPeriodField",
          "type": "string",
          "required": false,
          "description": "Date field for time-bucket histogram: checkedIn (default) or checkedOut."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)."
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)."
        }
      ],
      "exampleInput": "{ \"checkInStatus\": \"CheckedIn\", \"location\": \"Shelf B2\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 21, \"hits\": [ { \"sampleId\": \"s1...\", \"sampleCode\": \"SMP-3001\", \"checkInStatus\": \"CheckedIn\", \"location\": \"Shelf B2\" } ] }",
      "knownLimitations": "The only tool that returns physical sample records. Check-in status values: WaitingForCheckingIn, CheckedIn, CheckedOut."
    },
    {
      "name": "query_talent_crew",
      "title": "Query Talent & Crew",
      "module": "Talent & Crew",
      "description": "Search talent and resource users — both license users (internal team members) and resource users (freelancers, models, agencies). Use this to answer: who is on the team, who has a given skill, what agency a model belongs to, counts of talent by skill / employment type / status. User types: License (internal), Resource (external). Employment types: Permanent, Freelance. States: Enabled, Disabled, NotVerify, Deleted. Common skills: Photographer, Stylist, Model, Art Director, Hair & Makeup, Retoucher, Videographer, Producer, Assistant, Project Manager, Deliverable Owner, Digital Processing. Filter by name, job title, user type, employment type, skill, state, agency, short list, contact info, city, created date range. Pass `talentCrewIds` (JSON array of talent crew IDs from query_talent_crew_schedule or other tools) to look up specific records directly instead of re-querying by skill. Use human-readable names for all filters (resolved to IDs automatically). Each record includes a `deepLinkUrl` to the talent detail page in the web app. **CRITICAL — UI rendering ownership**: an inline widget handles record-list and aggregation responses, so your text must NOT duplicate them. Per response shape:   • `hits` with ≥1 talent → widget renders compact-card / table. Text = ONE brief sentence (e.g. 'Found 6 talents in the Christmas 24 short list.').   • Aggregation (`byAgency` / `bySkill` / `byStatus` / `byUserType` / `byEmploymentType`) → widget renders bar-chart sections. Text = ONE brief sentence summarising top buckets (e.g. '15 agencies, top 3: ACME (12), aaa (9), 234234 (8).').   • `hits` empty + `total > 0` (count-only, pageSize=0) → widget HIDES (0 height). Text MUST state the count plainly (e.g. 'Có **123** photographers đang active.').   • `hits` empty + `total = 0` → widget HIDES. Text = brief negative reply (e.g. 'Không có crew member nào match.'). Do NOT write a markdown table, list, gallery, or any field-by-field summary. Do NOT embed `![image](avatarUrl)` or `[Show Image](url)`. Do NOT include deep-link URLs in text — the widget already renders the name as a clickable anchor. Avatars are shown by default in compact-card and table views. Pass `includeAvatar=false` only when the caller explicitly wants to skip avatar resolution (e.g. perf-sensitive bulk queries). Still no markdown images in your text — the widget owns the visual. **\"Talent\" terminology** — `skill=\"Talent\"` is a PSEUDO-SKILL meaning Models + AI Models only (server-side OR-match). ✅ TRIGGER on standalone words: \"talent\", \"talents\", \"a talent\", \"the talents\", \"find a talent\", \"show me talents\", \"model agency\", or context implying Models specifically. Examples: 'Find a talent named Jane' → { name: 'Jane', skill: 'Talent' }; 'How many talents do we have?' → { skill: 'Talent', pageSize: 0 }. ❌ DO NOT TRIGGER on: 'Talent & Crew' / 'Talent and Crew' / 'talents and crew' — these are the FULL ENTITY NAME (License + Resource = whole pool). They contain the word \"Talent\" but mean the OPPOSITE — search broad pool, omit `skill`. Examples: 'Find Talent & Crew named Han' → { name: 'Han' } NO skill; 'How many Talent & Crew based in London' → { city: 'London' } NO skill. ❌ DO NOT TRIGGER for explicit roles (Photographers, Stylists, Producers, Hair & Makeup, Videographers, internal team) — pass the explicit skill name or omit the filter. When groupBy params are set, returns counts / aggregations instead of a list. Each hit includes a `rates` array (rateId, name, amount, currency, duration, rateType, agencyFee, payrollFee, isDefault, description) **only if the caller has the `Rates` screen permission**. Top-level `ratesAccess` is `granted` when rates are emitted and `denied` when they are gated out — when `denied`, do not assume the talent has no rates configured, the field is simply hidden by permission. **Rates are sensitive financial data — the widget HIDES them by default.** Even when `ratesAccess=granted` and the `rates` array is populated, the widget does NOT render the rates block in any view (compact card, table drill-down) unless `columns` explicitly includes 'rates'. The schedule tool widget never renders rates. This is a data-minimization safeguard against accidental leaks during screen-shares and casual lookups. To surface rates in the widget, set `columns: ['rates']` (or include 'rates' alongside other columns). You can still reason about rates in your text reply — but only when the user explicitly asked. **STRICT RULE for the `columns` parameter** — you MUST set it whenever the user names specific fields or uses narrowing words ('just', 'only', 'only show', 'just show', 'chỉ', 'chỉ hiển thị', 'khÃ´ng cần'). Do NOT fall back to defaults in those cases. Mapping examples:  • 'List 10 photographers and **just** show their skills' → call with arguments: { skill: 'Photographer', pageSize: 10, columns: ['skills'] }   • '5 models with their **rate info only**' → { skill: 'Model', pageSize: 5, columns: ['rates'] }   • '**Emails and phones** of all stylists' → { skill: 'Stylist', columns: ['email','phone'] }   • 'Which agency does each model belong to' → { skill: 'Model', columns: ['agencies'] }   • 'Talent based in London' → { city: 'London' }   • 'Talent in the Fashion Crew short list' → { shortListName: 'Fashion Crew' }   • 'Talent who are both Photographer AND Videographer' → { skill: 'Photographer|Videographer' }   • 'NotVerify accounts older than 30 days' → { state: 'NotVerify', createdTo: '<today-30d ISO>' }   • 'How many new talents joined in the last 3 months' → { createdFrom: '<today-3m ISO>', pageSize: 0 } (read total)   • 'List talents grouped by skill' (aggregation) → omit columns; the param has no effect on aggregation mode.   • Generic 'list/show me photographers' (no narrowing word) → omit columns; defaults to a broad set. Photo and Name are always rendered as anchors regardless of `columns`. Available column ids: jobTitle, type, employment, status, skills, agencies, email, phone, location, createdAt, pronouns, userCode, rates.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "talentCrewIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Talent crew IDs (JSON array) to look up specific records by ID — e.g. from query_talent_crew_schedule results. E.g. [\"id1\",\"id2\"]. When provided, other filters are still applied on top."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "description": "Generic name (contains, case-insensitive). OR-matches firstName, lastName, displayName — use this for AMBIGUOUS prompts like 'find a talent named Jane' where the user did NOT specify which name field. Do not combine with firstName/lastName/displayName."
        },
        {
          "name": "firstName",
          "type": "string",
          "required": false,
          "description": "First name (contains, case-insensitive). Use ONLY when the prompt explicitly narrows to first name (e.g. 'whose first name is Jane'). For ambiguous 'named Jane' prompts use the `name` param instead."
        },
        {
          "name": "lastName",
          "type": "string",
          "required": false,
          "description": "Last name (contains, case-insensitive). Use ONLY when the prompt explicitly narrows to last name. For ambiguous 'named Jane' prompts use the `name` param instead."
        },
        {
          "name": "displayName",
          "type": "string",
          "required": false,
          "description": "Display name (contains, case-insensitive). Use ONLY when the prompt explicitly references display name. For ambiguous 'named Jane' prompts use the `name` param instead."
        },
        {
          "name": "pronouns",
          "type": "string",
          "required": false,
          "description": "Pronouns"
        },
        {
          "name": "jobTitle",
          "type": "string",
          "required": false,
          "description": "Job title (contains)"
        },
        {
          "name": "userType",
          "type": "string",
          "required": false,
          "description": "User type: License, Resource"
        },
        {
          "name": "employmentType",
          "type": "string",
          "required": false,
          "description": "Employment type: Permanent, Freelance"
        },
        {
          "name": "userCode",
          "type": "string",
          "required": false,
          "description": "User code (exact)"
        },
        {
          "name": "skill",
          "type": "string",
          "required": false,
          "description": "Skill name (e.g. Photographer, Model). **Comma-separated = OR** (e.g. 'Photographer,Videographer' returns talents with EITHER skill — the natural 'Photographers and Videographers' union). **Pipe-separated = AND** (e.g. 'Photographer|Videographer' returns only talents with BOTH skills — multi-talent intersect). Single value works as before. Pseudo-skill 'Talent' resolves to Model OR AI Model server-side. Each name resolved to skill ID via master data cache."
        },
        {
          "name": "state",
          "type": "string",
          "required": false,
          "description": "State: Enabled, Disabled, NotVerify, Deleted"
        },
        {
          "name": "contactEmail",
          "type": "string",
          "required": false,
          "description": "Contact email (contains)"
        },
        {
          "name": "contactPhone",
          "type": "string",
          "required": false,
          "description": "Contact phone (contains)"
        },
        {
          "name": "agencyName",
          "type": "string",
          "required": false,
          "description": "Agency name (nested filter on agencies)"
        },
        {
          "name": "shortListName",
          "type": "string",
          "required": false,
          "description": "Short list name — exact name of a Talent short list (Collection with FunctionType=Shortlist). Returns talents who are members of that list. E.g. 'Fashion Crew'."
        },
        {
          "name": "city",
          "type": "string",
          "required": false,
          "description": "City (contains, case-insensitive)"
        },
        {
          "name": "createdFrom",
          "type": "string",
          "required": false,
          "description": "Created from (ISO date, inclusive). E.g. '2026-04-05' or '2026-04-05T00:00:00Z'. Combine with `createdTo` for a range, or use alone as a lower bound."
        },
        {
          "name": "createdTo",
          "type": "string",
          "required": false,
          "description": "Created to (ISO date, exclusive). Use to filter accounts older than a threshold — e.g. NotVerify users older than 30 days set `createdTo` to <today-30d>."
        },
        {
          "name": "groupByUserType",
          "type": "bool",
          "required": false,
          "description": "Group by user type — aggregation mode"
        },
        {
          "name": "groupByEmploymentType",
          "type": "bool",
          "required": false,
          "description": "Group by employment type — aggregation mode"
        },
        {
          "name": "groupBySkill",
          "type": "bool",
          "required": false,
          "description": "Group by skill — aggregation mode"
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by status — aggregation mode"
        },
        {
          "name": "groupByAgency",
          "type": "bool",
          "required": false,
          "description": "Group by agency — aggregation mode (nested)"
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        },
        {
          "name": "includeAvatar",
          "type": "bool",
          "required": false,
          "description": "If true (default), eagerly resolve avatarUrl for each hit so the widget renders avatars in compact-card and table views. Pass false only to skip the extra Contact API call."
        },
        {
          "name": "columns",
          "type": "JsonElement?",
          "required": false,
          "description": "Optional. Pick which columns the widget surfaces (applies to BOTH table view and compact card). Choose minimal set based on user intent: ['skills'] for skill questions, ['agencies'] for agency questions, ['email','phone'] for contact questions. **RATES HANDLING — STRICT RULE**: pass `['rates']` ONLY when the user EXPLICITLY mentions rates, cost, pricing, fees, budget, day rate, or asks to compare prices. Do NOT pass `['rates']` for ambiguous prompts like 'show profile', 'who is X', 'tell me about [name]', or for any list/team query that doesn't reference money. Default response (no `columns`) MUST NOT surface rates anywhere — including compact card and detail panel. Available column ids: jobTitle, type, employment, status, skills, agencies, email, phone, location, createdAt, pronouns, userCode, rates. Photo and Name are always shown as anchors. If omitted, defaults to [jobTitle, type, status, skills, agencies] (rates excluded). Aggregation mode ignores `columns`."
        },
        {
          "name": "view",
          "type": "string",
          "required": false,
          "description": "View mode: `widget` (default) renders the avatar table widget; `data` returns the same records under `data[]` with `hits=[]` (widget auto-hides) and SKIPS avatar resolution. **Use `view=data` ONLY when the result is being chained as input to another tool call (intermediate data extraction)**, NOT for user-facing answers. Example chain: extract talent IDs to feed into a follow-up query_talent_crew_schedule(talentCrewIds=[...]) call. Default `widget` is correct for ALL user-facing answers."
        }
      ],
      "exampleInput": "{ \"skill\": \"Photographer\", \"city\": \"London\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 8, \"hits\": [ { \"talentCrewId\": \"t-101\", \"displayName\": \"Jane Doe\", \"jobTitle\": \"Photographer\", \"userType\": \"Resource\", \"avatarUrl\": \"https://cdn.../t-101.jpg\" } ] }",
      "knownLimitations": "Rates are gated by the Rates screen (`ratesAccess: granted|denied`). The pseudo-skill 'Talent' resolves to Model OR AI Model server-side."
    },
    {
      "name": "query_talent_crew_schedule",
      "title": "Query Talent & Crew Schedule",
      "module": "Resourcing Calendar",
      "description": "**MANDATORY TRIGGERS** — when user prompt contains ANY of: 'available', 'availability', 'schedule', 'booking', 'confirmed', 'on hold', '1st hold', '2nd hold', 'next week', 'this week', 'on May/June/...', 'free on', 'busy on', 'OOO', 'out of office', 'unavailable on', 'inactive', 'haven't been booked', 'not booked in N days', 'access cleanup', 'dormant', 'idle freelancers' → you MUST call this tool. Do NOT respond with markdown tables, lists, or hallucinate data. The widget will render the answer visually. **Inverse query** (find talents WITHOUT bookings) → use `onlyInactive=true` paired with `dateFrom/dateTo` for the inactivity window. Common phrasings: 'freelancers not booked in 60+ days' → `onlyInactive=true, dateFrom=<60d ago>, dateTo=<today>` (chain with `query_talent_crew(employmentType=Freelancer, view=data)` first to scope pool to freelancers). **NEGATIVE TRIGGER — route to `query_planning` instead** when the prompt names a free-form production SUPPORT role like cater, catering, lunch, food, runner, driver, PA, DIT, dog handler, medic, security, prop master, set decorator, etc. These are NOT skills (skills = Photographer/Stylist/Model/Art Director/Hair&Makeup/Assistant/Videographer/DigitalProcessing). They are Custom Resource labels attached inline to planning sessions. **Do NOT pass them as `skill=...` here → the master skill catalog will not match them and the tool will return 0**. Use `query_planning(includeCustomResources=true)` instead. Query Talent & Crew scheduling data from the Resourcing Calendar — User Availability (Available/OutOfOffice/Unavailable set by the user) and Resource Bookings (1stHold/2ndHold/Confirmed/Unavailable set by manager). Use for: who is available next week, who is confirmed for a shoot date, stale holds, overbooking conflicts, per-record schedule, capacity overview. **This tool ALSO serves as a list view of talents matching the skill filter** — do NOT pre-call query_talent_crew when user prompt combines list+schedule intent (e.g. 'list all Models then their schedule next week'). Pass `skill` directly here for both the list and the schedule in ONE call. **Chain mode**: when the result is being passed as input to another tool (intermediate data extraction, NOT user-facing), set `view=data` to suppress widget render and skip avatar resolution (saves ~150ms). Default `view=widget` is correct for ALL user-facing answers. Requires dateFrom + dateTo. Scope is OPTIONAL. **Do NOT chain via query_talent_crew to pre-resolve IDs by skill.** Pass `skill` directly here — the tool resolves talents internally. When you also pass `availabilityStatus` or `bookingStatus`, the tool runs **status-first**: scans the whole studio crew pool (cap 500), filters by status, then post-filters by skill — this catches talents that a skill-first scope (cap 50) would miss. Use cases for unscoped queries: 'how many crew are unavailable on May 8?' (set pageSize=0 for count-only), 'who is Out of Office next week?'. **Name-scoped queries — mandatory chain.** When the user names a SPECIFIC talent (proper name: 'AloHa NN', 'John Photographer', 'show schedule for Tien'): FIRST call `query_talent_crew(name=..., view=data)` to resolve name → ID(s), then pass those as `talentCrewIds` here. Do NOT call this tool unscoped expecting to find the named talent in the result list — that wastes a round trip and dumps the whole crew on the user. **Session-scoped queries — chain order is mandatory.** When the user mentions a session: FIRST call `query_planning` to resolve the session — pick the right session param: `sessionCode` if user said it's a code, `sessionName` if a name, `session` if ambiguous. Then use the response's `id` (UUID) as `sessionId` here, and `timeSlots` to derive `dateFrom`/`dateTo`. Do NOT call this tool first with a raw code/name — it requires a UUID. **Do NOT drop `sessionId` as a fallback** when a session-scoped query returns 0. Zero IS the answer — it means the session has no records matching the filters. Report 0 to the user (e.g. 'no 1st/2nd Hold bookings for that session — the crew are Confirmed instead'). To find out what status the session crew DO have, drop the `bookingStatus` filter while keeping `sessionId` — never the other way around. For session team composition ('who is on session X') use query_planning instead. sessionId here filters bookings linked to a specific session (1st Hold / Confirmed per shoot). Availability statuses (User Availability set by the user): Available, OutOfOffice, Unavailable. Booking statuses (Resource Booking set by manager): typically Option, Contacted, Confirmed, 1st Hold (FirstHold), 2nd Hold (SecondHold), Released, Cancelled, Unavailable — exact list depends on studio config (see _debug.cachedBookingStatusNames). Pass the canonical name (case-insensitive) — server resolves to dynamic statusId. **Multi-status filter — `bookingStatuses` / `availabilityStatuses`**: pass a JSON array of names (e.g. `bookingStatuses=[\"Confirmed\",\"FirstHold\",\"SecondHold\"]`) when the intent covers multiple statuses. Records matching ANY listed status pass. Use the plural form whenever the user phrasing implies a set, not a single canonical status. **Singular vs multi → decide by user word, not by intent**. RULE: if user prompt contains an explicit canonical status word ('confirmed', '1st hold', 'first hold', '2nd hold', 'second hold', 'option', 'contacted', 'released', 'cancelled') → use SINGULAR `bookingStatus=<that word>`. Examples: 'who is **confirmed** today' → `bookingStatus=Confirmed`. 'show me **1st holds** for May 14' → `bookingStatus=FirstHold`. 'anyone **on hold** today' (vague — 1st or 2nd) → `bookingStatuses=[\"FirstHold\",\"SecondHold\"]`. Only use the multi-status form when the user prompt is VAGUE about status: 'who is **working/on-floor/on the books/scheduled/booked** today' → active bookings → `bookingStatuses=[\"Confirmed\",\"FirstHold\",\"SecondHold\"]`. **Heuristic**: if the user typed a status word verbatim, that's the filter → singular. If they typed a generic 'working/scheduled' verb, multi. Don't mix. **Booking-vs-Availability AND-gate (LH-50337 follow-up)**: when ANY booking-status filter is set, the tool now narrows availability records to talents that survived the booking filter (and vice versa). So `bookingStatuses=[...]` will NOT leak availability-only rows for talents without a matching booking. Translation: filtering 'Confirmed today' returns only talents who actually have a Confirmed booking on that day, even though their availability records overlap. When groupBy params set, returns bucket counts instead of list. **Rendering rule**: each record carries a `deepLinkUrl`. When presenting results in text, ALWAYS wrap `displayName` as a clickable markdown link: `[displayName](deepLinkUrl)`. Never print plain names. If `displayName` is empty, fall back to talentCrewId or userCode (still wrapped). **UI rendering**: paired widget renders avatar table with status pills + inline date ranges + clickable names (Availability / Booking / On-set Role columns auto-shown when relevant data present). Per response shape:   • `hits` with ≥1 talent → widget renders compact-card / table. Text = ONE brief sentence summary PLUS optionally ONE useful insight you spot in the data (e.g. 'Found 6 confirmed photographers for May 8 — note: oanh eu has overlapping bookings May 7-15.'). Do not list each row in text — widget shows the data.   • Aggregation (`byAvailabilityStatus` / `byBookingStatus`) → widget renders bar chart. Text = ONE brief sentence summarising buckets.   • `hits` empty + `total > 0` (count-only, pageSize=0) → widget HIDES. Text MUST state the count plainly.   • `hits` empty + `total = 0` → widget HIDES. **IMPORTANT fallback**: before replying 'no one is booked', call `query_planning(dateFrom=<same>, dateTo=<same>, groupByTeamMember=true)` to check if talent are assigned on sessions without bookings. If `query_planning` returns teamMembers, present those results to the user (e.g. 'No bookings found, but these talent are assigned on sessions this week:'). Only reply 'no one is planned' if BOTH tools return empty. **Conflict API unavailability**: if `_debug.conflictApiAvailable === false`, the booking conflict service is down for this query. ALL hits will have `hasConflict=false` regardless of actual state. The widget renders a footer note, but you SHOULD also append a brief warning to your text reply (e.g. 'Note: conflict warnings are temporarily unavailable — verify holds manually'). **INACTIVE BOOKINGS — STRICT RULE**: by default the tool excludes `Cancelled` and `Released` bookings from results (active-schedule hygiene). If `_debug.inactiveCount.total > 0` in the response, the widget rendered fewer rows than exist server-side. Mention this in the text reply (e.g. 'X cancelled / Y released bookings hidden — let me know if you want to see them.'). Pass `includeInactiveBookings=true` ONLY when the user EXPLICITLY mentions cancelled, released, inactive, or 'all bookings' (e.g. 'show all bookings including cancelled', 'were any of Tien's holds released?'). Do NOT pass it for generic 'schedule for X' or 'who's available' queries. **Rates are NEVER shown via this tool.** Schedule queries are about availability and bookings, not pricing. The widget for this tool does not render rates in any view (single, list, or drill-down). If the user asks about rates after a schedule query, chain to `query_talent_crew(talentCrewIds=[<from schedule result>], columns=['rates'])` instead of re-calling the schedule tool. Do NOT write a markdown table, do NOT chain to get_talent_crew_preview, do NOT fabricate avatar URLs in text. Do NOT include deep-link URLs in text — the widget renders the name as a clickable anchor.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "talentCrewIds",
          "type": "JsonElement?",
          "required": false,
          "description": "Talent crew IDs (JSON array) from query_talent_crew. E.g. [\"id1\",\"id2\"]. Max 50."
        },
        {
          "name": "talentCrewId",
          "type": "string",
          "required": false,
          "description": "Single talent crew ID — for per-record schedule view."
        },
        {
          "name": "skill",
          "type": "string",
          "required": false,
          "description": "Skill name to scope talent pool e.g. Photographer. Resolved via talentcrew ES. Max 50 results."
        },
        {
          "name": "dateFrom",
          "type": "string",
          "required": false,
          "description": "Start date ISO e.g. 2026-06-01 (required)."
        },
        {
          "name": "dateTo",
          "type": "string",
          "required": false,
          "description": "End date ISO e.g. 2026-06-07 (required)."
        },
        {
          "name": "availabilityStatus",
          "type": "string",
          "required": false,
          "description": "Filter User Availability status: Available, OutOfOffice, Unavailable."
        },
        {
          "name": "availabilityStatuses",
          "type": "JsonElement?",
          "required": false,
          "description": "Multi-status User Availability filter — JSON array of canonical names, e.g. [\"Available\",\"OutOfOffice\"]. Records matching ANY listed status pass. Takes precedence over `availabilityStatus` when both supplied."
        },
        {
          "name": "bookingStatus",
          "type": "string",
          "required": false,
          "description": "Filter Resource Booking status — case-insensitive. Common values: Option, Contacted, Confirmed, FirstHold (or '1st Hold'), SecondHold (or '2nd Hold'), Released, Cancelled, Unavailable. Studio-specific list available in _debug.cachedBookingStatusNames. NOTE: by default Cancelled and Released are hidden (data hygiene); pass them explicitly here to see only those, or pass `includeInactiveBookings=true` to see them alongside active bookings."
        },
        {
          "name": "bookingStatuses",
          "type": "JsonElement?",
          "required": false,
          "description": "Multi-status Resource Booking filter — JSON array of canonical names, e.g. [\"Confirmed\",\"FirstHold\",\"SecondHold\"] for active bookings. Records matching ANY listed status pass. Takes precedence over `bookingStatus` when both supplied. **Recommended over `bookingStatus` for 'who is working today / on-floor / scheduled' queries** — pass [\"Confirmed\",\"FirstHold\",\"SecondHold\"] to capture all real holds in one call instead of inferring `Confirmed` only."
        },
        {
          "name": "includeInactiveBookings",
          "type": "bool",
          "required": false,
          "description": "Include Cancelled and Released bookings in results. Default false (these statuses clutter the active-schedule view). Pass true ONLY when user EXPLICITLY mentions cancelled, released, inactive, or 'all bookings'. If `bookingStatus` is set explicitly (e.g. to 'Cancelled'), that takes precedence and this flag becomes irrelevant."
        },
        {
          "name": "onlyInactive",
          "type": "bool",
          "required": false,
          "description": "**Inverse mode** — return ONLY talents in the pool that have ZERO bookings overlapping [dateFrom..dateTo]. Use for 'who hasn't been booked recently' / 'inactive freelancers' / 'access cleanup' queries (e.g. 'which freelancers haven't been booked in 60+ days'). The pool is determined by the usual sources (talentCrewIds, sessionId, or the full studio crew). When set, the tool clears all booking/availability records before rendering, forces phantom emission, and bypasses status filters (bookingStatus*/availabilityStatus* become noise). Pair with `dateFrom=<60d ago>, dateTo=<today>` for the canonical use case. Skill / employmentType narrowing: chain via `query_talent_crew(employmentType=Freelancer, view=data)` first to get IDs, then pass as `talentCrewIds` here. Default false — normal mode returns talents WITH records."
        },
        {
          "name": "sessionId",
          "type": "string",
          "required": false,
          "description": "Filter bookings linked to a specific session — must be the session UUID. If the user gave a session code, name, or ambiguous identifier, you MUST first call `query_planning` (use `sessionCode` for explicit codes, `sessionName` for explicit names, `session` for ambiguous) to resolve it; pass the returned `id` here, and use `timeSlots` to set dateFrom + dateTo. Passing a non-UUID returns an error."
        },
        {
          "name": "bookingCreatedBefore",
          "type": "string",
          "required": false,
          "description": "ISO date — return only bookings created before this date (surface stale holds)."
        },
        {
          "name": "groupByAvailabilityStatus",
          "type": "bool",
          "required": false,
          "description": "Aggregation: group by User Availability status and return counts."
        },
        {
          "name": "groupByBookingStatus",
          "type": "bool",
          "required": false,
          "description": "Aggregation: group by Resource Booking status and return counts."
        },
        {
          "name": "hasConflict",
          "type": "bool",
          "required": false,
          "description": "Health: return only talents with active overbooking conflicts (conflictStatusId 105/106/107)."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results in list mode (ignored in aggregation). Set to 0 for COUNT-only mode — response keeps `totalAvailabilities` + `totalBookings` (full counts after status/session filtering) but omits the item arrays. Use this when the user asks 'how many ...' without needing per-record details."
        },
        {
          "name": "view",
          "type": "string",
          "required": false,
          "description": "View mode: `widget` (default) renders the avatar table widget; `data` returns the same records under `data[]` with `hits=[]` (widget auto-hides) and SKIPS avatar resolution. **Use `view=data` ONLY when the result is being chained as input to another tool call (intermediate data extraction)**, NOT for user-facing answers. Example chain: extract talent IDs to feed into a follow-up query_asset(talentCrewId=...) call. Default `widget` is correct for ALL user-facing answers."
        }
      ],
      "exampleInput": "{ \"dateFrom\": \"2026-06-10\", \"dateTo\": \"2026-06-20\", \"bookingStatus\": \"Confirmed\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 5, \"hits\": [ { \"talentCrewId\": \"t-101\", \"displayName\": \"Jane Doe\", \"availabilityStatus\": \"Available\", \"bookingStatus\": \"Confirmed\", \"onSetRole\": \"Photographer\", \"hasConflict\": false } ] }",
      "knownLimitations": "Availability + bookings grouped by talent. Status is filtered in-memory (the planning backend has no status filter); skill is applied as a post-filter."
    },
    {
      "name": "query_task",
      "title": "Query Task",
      "module": "Workflow",
      "description": "Search tasks (individual workflow steps). Status: New, Todo, InProgress, Rejected, QCRejected, Failed, Bypassed, Done. Use names for step, production type, assignee, vendor, workspace. Can filter by product/job/workspace and includes product, job, assignee info in results. When groupBy params set, returns counts/aggregations instead of list. For any 'per workspace' or 'per client' breakdown, use groupByWorkspace=true.",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "taskId",
          "type": "string",
          "required": false,
          "description": "Task ID (exact match)"
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "Step status name"
        },
        {
          "name": "step",
          "type": "string",
          "required": false,
          "description": "Step name: Photography, InternalPostProduction, FinalSelection, etc."
        },
        {
          "name": "productionType",
          "type": "string",
          "required": false,
          "description": "Production type name or ID"
        },
        {
          "name": "assignee",
          "type": "string",
          "required": false,
          "description": "Assignee name or user ID"
        },
        {
          "name": "postProductionVendor",
          "type": "string",
          "required": false,
          "description": "Post-production vendor name or ID"
        },
        {
          "name": "jobCode",
          "type": "string",
          "required": false,
          "description": "Job code"
        },
        {
          "name": "jobName",
          "type": "string",
          "required": false,
          "description": "Job name"
        },
        {
          "name": "productCode",
          "type": "string",
          "required": false,
          "description": "Product code (exact)"
        },
        {
          "name": "productName",
          "type": "string",
          "required": false,
          "description": "Product name (exact)"
        },
        {
          "name": "product",
          "type": "string",
          "required": false,
          "description": "Search product by code or name (OR)"
        },
        {
          "name": "dateFrom",
          "type": "string",
          "required": false,
          "description": "Finished date from (ISO)"
        },
        {
          "name": "dateTo",
          "type": "string",
          "required": false,
          "description": "Finished date to (ISO)"
        },
        {
          "name": "workspace",
          "type": "string",
          "required": false,
          "description": "Workspace/client name or ID"
        },
        {
          "name": "startedFrom",
          "type": "string",
          "required": false,
          "description": "Started date from (ISO)"
        },
        {
          "name": "startedTo",
          "type": "string",
          "required": false,
          "description": "Started date to (ISO)"
        },
        {
          "name": "groupByStep",
          "type": "bool",
          "required": false,
          "description": "Group by step — aggregation mode"
        },
        {
          "name": "groupByStatus",
          "type": "bool",
          "required": false,
          "description": "Group by status — aggregation mode"
        },
        {
          "name": "groupByAssignee",
          "type": "bool",
          "required": false,
          "description": "Group by assignee — aggregation mode"
        },
        {
          "name": "groupByVendor",
          "type": "bool",
          "required": false,
          "description": "Group by vendor — aggregation mode"
        },
        {
          "name": "groupByProductionType",
          "type": "bool",
          "required": false,
          "description": "Group by production type — aggregation mode"
        },
        {
          "name": "groupByPeriod",
          "type": "string",
          "required": false,
          "description": "Group by period: hour, day, week, month — aggregation mode"
        },
        {
          "name": "groupByWorkspace",
          "type": "bool",
          "required": false,
          "description": "Group by workspace/client — aggregation mode. Use for any 'per workspace', 'per client', 'by workspace' question."
        },
        {
          "name": "pageSize",
          "type": "int",
          "required": false,
          "description": "Max results (ignored in aggregation)"
        },
        {
          "name": "page",
          "type": "int",
          "required": false,
          "description": "Page (0-based, ignored in aggregation)"
        }
      ],
      "exampleInput": "{ \"status\": \"InProgress\", \"step\": \"InternalPostProduction\", \"assignee\": \"John Smith\", \"pageSize\": 10 }",
      "exampleOutput": "{ \"total\": 11, \"hits\": [ { \"taskId\": \"wk1...\", \"step\": \"InternalPostProduction\", \"status\": \"InProgress\", \"assignee\": \"John Smith\" } ] }",
      "knownLimitations": "One workflow step per row. Status values: New, Todo, InProgress, Rejected, QCRejected, Failed, Bypassed, Done."
    },
    {
      "name": "query_workspace",
      "title": "Query Workspace",
      "module": "Workspaces",
      "description": "List workspaces (clients) in the studio with their calendar & timezone settings. Returns: id, name, isDefault, isEnabled, timezoneDisplayName, startDayOfWeek, firstWeekOfYear. **Use cases**: (1) Enumerate workspace names/IDs before grouping or filtering other entities by workspace. (2) Get a workspace's timezone and calendar settings. Pass 'keyword' for server-side partial name search; omit to get all workspaces. Use workspace IDs from the result to pass as 'workspace' filter to other query tools (query_ecomm_production, query_ecomm_job, query_ecomm_product_request, etc.).",
      "readOnly": true,
      "idempotent": true,
      "destructive": false,
      "parameters": [
        {
          "name": "keyword",
          "type": "string",
          "required": false,
          "description": "Search workspace by name (partial match, case-insensitive). Omit to list all."
        }
      ],
      "exampleInput": "{ \"keyword\": \"acme\" }",
      "exampleOutput": "{ \"total\": 2, \"hits\": [ { \"id\": \"8a...\", \"name\": \"Acme Studio\", \"isDefault\": true, \"isEnabled\": true, \"timezoneDisplayName\": \"Europe/London\", \"startDayOfWeek\": \"Monday\" } ] }",
      "knownLimitations": "Returns workspace/client configuration only. Use the returned `id`s as the `workspace` filter on the other query tools."
    },
    {
      "name": "send_feedback",
      "title": "Send Feedback",
      "module": "Platform",
      "description": "Send feedback to the Creative Force team. Types: result_feedback (rate a result), unsolved_question (couldn't get an answer), feature_request (want new capability).",
      "readOnly": false,
      "idempotent": false,
      "destructive": false,
      "parameters": [
        {
          "name": "type",
          "type": "string",
          "required": true,
          "description": "Feedback type: result_feedback, unsolved_question, or feature_request"
        },
        {
          "name": "message",
          "type": "string",
          "required": true,
          "description": "Your comment, question, or request"
        },
        {
          "name": "rating",
          "type": "string",
          "required": false,
          "description": "Rating (for result_feedback): correct, incorrect, or partial"
        },
        {
          "name": "relatedToolName",
          "type": "string",
          "required": false,
          "description": "Which tool produced the result"
        },
        {
          "name": "relatedToolInput",
          "type": "string",
          "required": false,
          "description": "Parameters used in the related tool call"
        },
        {
          "name": "relatedToolOutput",
          "type": "string",
          "required": false,
          "description": "Summary of what the tool returned"
        }
      ],
      "exampleInput": "{ \"type\": \"result_feedback\", \"rating\": \"correct\", \"message\": \"Job list was accurate\", \"relatedToolName\": \"query_ecomm_job\" }",
      "exampleOutput": "{ \"status\": \"received\", \"type\": \"result_feedback\" }",
      "knownLimitations": "The only tool that writes anything — it sends your feedback to the Creative Force team and writes nothing to your studio's data."
    }
  ]
}